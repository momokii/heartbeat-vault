# ── Environment ──
# Active environment: development | staging | production
APP_ENV=development

# ── App ──
# APP_NAME=heartbeat-vault
# APP_URL=http://localhost:3000
# LOG_LEVEL=debug

# ── Database (fill in once the stack is chosen) ──
# DATABASE_URL=postgresql://user:changeme@localhost:5432/heartbeat_vault

# ── Auth (fill in once the auth design is decided) ──
# JWT_SECRET=change-me-to-a-long-random-value
# SESSION_SECRET=change-me-to-a-long-random-value

# ── External services (add keys as integrations are introduced) ──
# EXTERNAL_API_KEY=your-key-here
