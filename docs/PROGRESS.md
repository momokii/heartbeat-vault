# Heartbeat Vault — Progress Tracker

> Memory across long sessions. Read `CLAUDE.md`, `docs/BRIEF.md`, then this file first on resume.
> Updated at each phase gate per Brief Section 14.

## Phase 0: Discovery and Design Gate — IN PROGRESS (started 2026-09-20)

- [x] Save brief verbatim to `docs/BRIEF.md`
- [x] Init git repo (pre-existing bootstrap commits `651fcae`, `693d6e5` on `main`)
- [x] Research best practices (Sections 5–12 + 7.1) — 5 librarian briefs collected 2026-09-20
- [x] Clarifying questions asked — user answered 2026-09-20 (see Key decisions)
- [x] Design document produced (`docs/DESIGN.md`: architecture, threat model, data model, crypto, ADR list)
- [ ] User approval received — GATE, no implementation before this
- [ ] Assumptions recorded in `docs/ASSUMPTIONS.md` (defaults only where user cannot answer)
- [ ] Commit Phase 0 gate (Conventional Commits)

## Phase 1: Repo and tooling foundation — IN PROGRESS (started 2026-09-20, T1.1 monorepo skeleton)

- Gate: monorepo layout, lint/typecheck/test tooling, Docker base, `docs/PROGRESS.md` updated, committed.
- [x] T1.1 monorepo skeleton — pnpm workspaces + Turborepo + tsconfig.base.json strict + 6 package skeletons (no app code)
- [x] T1.2 lint/typecheck/format/hooks — ESLint 9.39.5 flat + typescript-eslint 8.70.0 + globals 16.5.0 + eslint-config-prettier 10.1.8 + Prettier 3.9.8 + lint-staged 17.5.1 + Husky 9.1.7 + .editorconfig; `pnpm lint` (eslint .), `pnpm typecheck` (turbo → tsc --noEmit), `pnpm format:check` (prettier --check) pass; lint-staged + Husky pre-commit (`pnpm exec lint-staged`); all deps pinned exact; `pnpm audit` logged in DECISIONS_LOG 2026-09-20 (2 turbo advisories noted); `.claude/ENVIRONMENT_GUIDE.md` updated; no `as any`/`@ts-ignore`
- [x] T1.3 Docker base (standard profile) — `docker-compose.yml` (api + postgres:17-alpine@sha256:18cfe... + caddy:2-alpine@sha256:de23de... all pinned, no `latest`), `docker-compose.override.yml` (dev: 127.0.0.1-bound DB/API/admin ports, LOG_LEVEL debug), `docker-compose.prod.yml` (restart unless-stopped, resource limits, no dev mounts/ports, json-file rotation), `apps/api/Dockerfile` multi-stage node@sha256:b6f26b... non-root `USER node` + `tini` + placeholder `server.mjs` (`/health` 200, TODO Phase 3+), `Caddyfile` (`tls internal` LAN default, HSTS/CSP/headers, `reverse_proxy api:3000`), `.env.example` extended (all ports, DB/app/auth/MASTER_KEY, TLS options, channel placeholders); healthchecks `pg_isready` + caddy admin ping + api Node http; `docker compose config` (both base and prod) valid; `docker compose up -d --build` healthy (all 3); DB internal-only (no ports in base/prod, 127.0.0.1-only in override); containers least-privilege; staged only, no commit (2026-09-20)
- [x] T1.4 ADRs 001-002 plus docs scaffold — `docs/adr/001-monorepo-pnpm-turborepo.md`, `docs/adr/002-postgres-as-queue.md`, `docs/adr/README.md`, stub `docs/ARCHITECTURE.md` + `docs/SECURITY.md` (each one paragraph scope plus pointer to `docs/DESIGN.md`, no invented details); grounded in `docs/DESIGN.md` §§1-2 and `docs/BRIEF.md` §§7/7.1; prose only, no code or Docker or ADRs 003-010

## Phase 2: Data model and crypto core — IN PROGRESS (T2.1 DB done, T2.2-2.4 envelope/KDF/asymmetric done 2026-09-20)

- Envelope encryption (per-secret DEK + master key), Argon2id, AEAD (AES-256-GCM / XChaCha20-Poly1305), rotation.
- Gate: unit + known-answer-vector crypto tests pass, coverage threshold enforced.
- [x] T2.1 DB package schema + migrations — `packages/db/src/schema.ts` (14 tables per DESIGN.md §3: users/recipients/switches/sealed_payloads/heartbeats/trigger_jobs/vault_waits/delivery_jobs/dead_letter_jobs/scheduler_heartbeat/audit_log/invites/sessions/app_config; all timestamptz `clock_timestamp()`, bytea via customType, intervals, checks, FK cascades, UNIQUEs) + `drizzle.config.ts` + `drizzle/0000_brown_titania.sql` (`CREATE EXTENSION pgcrypto`, roles `migrator`/`app`, least-privilege GRANTs, `REVOKE UPDATE,DELETE ON audit_log FROM app`, default privileges) + `src/migrate.ts` runner (drizzle-orm migrator) + `src/seed.ts` idempotent `app_config` defaults only (ON CONFLICT DO NOTHING) + `test/helpers.ts` (`PostgreSqlContainer postgres:17-alpine` pinned via `@testcontainers/postgresql@10.14.0`, `createTestDb`/`getDb`/`truncateAll`/`destroyTestDb`) + `src/schema.test.ts` vitest+Testcontainers 7 tests (tables exist, UNIQUE(switch_id,deadline_at) enforced, audit_log REVOKE via non-superuser app_tester, timestamptz UTC, singleton check, sealed_payloads unique, idempotency_key unique); TDD RED→GREEN; `drizzle-orm@0.45.2`+`pg@8.13.3` exact, `drizzle-kit@0.31.4`+`testcontainers@10.14.0`+`@testcontainers/postgresql@10.14.0` pinned; `pnpm --filter @heartbeat-vault/db typecheck` clean strict no `as any`; `pnpm --filter @heartbeat-vault/db test` 7/7 green on real Postgres; `pnpm audit` 2026-09-20 logged (turbo GHSA-hcf7/GHSA-3qcw pre-existing, undici/tar-fs/esbuild via testcontainers dev-only noted); `.prettierrc.json` singleQuote→true to satisfy 2-space single-quote 100-width; no crypto/auth/scheduler code, no git commit (2026-09-20)
- [x] T2.2 envelope AEAD core — `packages/crypto/src/envelope.ts` TDD RED→GREEN (XChaCha20-Poly1305 via `@noble/ciphers@2.4.0` pinned, 32 B DEK per encrypt memzero, KEK wrap AAD `{kid,kekVersion,tenantId,switchId}`, version 1 prefix, constant-time tag via `equalBytes`); `envelope.test.ts` 9 tests (round-trip, 1-bit tag flip, ciphertext flip, wrong AAD, wrong KEK, nonce uniqueness 1000, version tamper, empty + 64KB); `pnpm --filter @heartbeat-vault/crypto typecheck` clean (strict, no `as any`); `pnpm audit` no new advisories on new deps; `DESIGN-NOTE.md` (ADR-005 draft + RED paste); no DB/KDF/Shamir code (T2.3/T2.4 deferred)
- [x] T2.3 KEK rotation + Argon2id KDF — `packages/crypto/src/kdf.ts` (`hashPassword`/`verifyPassword`/`deriveKEK`/`deriveKEKAsync`/`needsRehash` via `@node-rs/argon2@2.2.1` pinned, OWASP `m=19456,t=2,p=1`, 16 B salt, 32 B output, pepper `KEK_PEPPER` fail-closed via `requirePepper` option, no default) + `packages/crypto/src/rotation.ts` (`rotateKEK` pure re-wrap `wrappedDEK` + `needsRotation`, batch-friendly, idempotent, AAD-aware) TDD RED→GREEN (RED: `kdf.js`/`rotation.js` missing → 2 failed, 9 envelope passed; GREEN: 21 passed — envelope 9 + kdf 7 + rotation 5: PHC round-trip, wrong password, malformed PHC throws, needsRehash old m=4096, pepper changes output, missing pepper throws, derive deterministic 32B, re-wrap preserves decryptability, idempotent double-rotate, old KEK retained, needsRotation, batch); `pnpm --filter @heartbeat-vault/crypto typecheck` clean (strict, verbatimModuleSyntax workaround); `pnpm audit` 2026-09-20 no new advisory for `@node-rs/argon2@2.2.1` (pre-existing: turbo GHSA-hcf7+GHSA-3qcw, undici via testcontainers); `DESIGN-NOTE.md` ADR-005 extended + RED paste; envelope.ts read-only (index.ts exports added)
- [x] T2.4 asymmetric + Shamir — `packages/crypto/src/asymmetric.ts` (X25519 `libsodium-wrappers@0.8.4` pinned, `generateKeyPair`/`sealForRecipient`/`openSealedBox`/`sealForMany` 48B overhead, anonymous) + `packages/crypto/src/sharing.ts` (Shamir `shamir-secret-sharing@0.0.4` pinned, `splitSecret(n=5,t=3)`/`combineShares` validation `2<=t<=n<=255`, `createCommitment=BLAKE2b` via `@noble/hashes@1.8.0` + `verifyCommitment` constant-time); `asymmetric.test.ts` 5 tests (round-trip, wrong key fails, tamper fails, multi-recipient each opens, 48B overhead) + `sharing.test.ts` 5 tests (n=5,t=3, <t fails commitment, corrupt share fails, invalid n/t throws, commitment helper); TDD RED→GREEN 19 passed; `pnpm --filter @heartbeat-vault/crypto exec vitest run src/envelope.test.ts src/asymmetric.test.ts src/sharing.test.ts` + `typecheck` clean strict; `DESIGN-NOTE.md` updated (ADR-006 draft, custody v1 vs deferred); `pnpm audit` no new advisories on new deps (turbo/vite/tar-fs advisories pre-existing); envelope.ts untouched (2026-09-20)
- [x] T2.5 KAT + differential + tamper + coverage gate — `packages/crypto/src/vectors.test.ts` (33 tests: XChaCha20-Poly1305 differential noble↔libsodium `crypto_aead_xchacha20poly1305_ietf_*` 4 tests incl. identical ciphertext, HKDF-SHA256 RFC 5869 §A.1 TC1 KAT `3cb25f…865` + differential vs `node:crypto hkdfSync` + random/empty cases, HMAC-SHA256 RFC 4231 TC1 `b0344c61…` + TC2 `5bdcc1…` + differential vs `createHmac`, Argon2id @node-rs/argon2 hash→verify + hashRawSync deterministic + cross-impl to libsodium `crypto_pwhash_str_verify` with documented fallback (libsodium-wrappers 0.8.4 lacks pwhash export — differential is sync vs async + PHC parse, never fabricated), Shamir round-trip 2-of-2/2-of-3/3-of-5 + share-length + quorum, Wycheproof-style tamper matrix 7 classes tag/ct/wrappedCT/nonce/nonce-length/AAD/key-length/version + oracle discipline generic `decrypt failed`); coverage-fill 8 tests (deriveKEKAsync vs sync, KDF/rotation/asymmetric/sharing/envelope validation, dek-length crafted 16B path); `packages/crypto/vitest.config.ts` coverage `provider v8` `all:true` thresholds `lines:90 functions:90 branches:80 statements:90` + `reporter text/lcov/html`; `packages/crypto/package.json` `@vitest/coverage-v8@5.0.1` pinned exact + `test:coverage` `vitest run --coverage`; root `package.json` + `turbo.json` `test:coverage`; `pnpm --filter @heartbeat-vault/crypto test` 64/64 green (31 existing +33 new), `pnpm --filter @heartbeat-vault/crypto exec vitest run --coverage` 94.5% lines (95/91.5/100 func) gate passes, `pnpm test:coverage` green, `typecheck` clean strict, no impl changes (tests + config only); `DESIGN-NOTE.md` CI-blocking note + audit; `pnpm audit` no new advisory for `@vitest/coverage-v8@5.0.1` (turbo/vite/tar-fs pre-existing) (2026-09-20)

## Phase 3: Auth, bootstrap, roles — DONE (T3.1–T3.4 complete 2026-09-20)

- One-time expiring setup token, first-account-becomes-admin, setup endpoint permanently disabled; admin-invite default; Argon2id, TOTP 2FA, WebAuthn/passkeys, recovery codes, sessions/revocation, rate limit, lockout, audit log.
- [x] T3.1 bootstrap: API server scaffold + one-time setup token + permanent 410 — `apps/api/src/server.ts` (`buildServer(pool)` Fastify 5.6.2 + @fastify/helmet 13.1.1 + @fastify/cookie 11.1.1, `bodyLimit` 1 MB, `GET /api/health` 200, helmet `x-content-type-options`/`x-frame-options`, trustProxy), `apps/api/src/lib/setup-token.ts` (`generateSetupToken` 32 B `crypto.randomBytes` base64url + `hashSetupToken` SHA-256 hex + constant-time `timingSafeEqual`), `apps/api/src/routes/setup.ts` (`POST /api/setup` zod email/password>=12 generic 400, SHA-256(token)==app_config.setup_token_hash + `setup_token_expires_at`>now + `setup_completed=false` in `BEGIN...FOR UPDATE...COMMIT`, Argon2id `hashPassword` via `@heartbeat-vault/crypto`, admin insert + `setup_completed=true` + `setup_token_hash=''` + `setup_token_consumed_at`=clock_timestamp + `audit_log` row, thereafter all calls 410 Gone), `apps/api/src/setup.test.ts` 9 tests via Testcontainers `postgres:17-alpine` + `vitest@5.0.1` (health+headers, happy→410+admin+410, wrongToken 400, expired 410/400, weakPassword 400, invalidEmail 400, hash-only storage hex64, permanent410 unreachable, base64url token shape); TDD RED→GREEN (RED missing module, GREEN 9/9), `pnpm --filter @heartbeat-vault/api typecheck` clean strict, `lint` clean, `prettier` 100-width singleQuote; `pnpm audit` no new high on api deps (fastify/helmet/cookie/zod/pg clean; pre-existing turbo/tar-fs/undici via testcontainers noted); Dockerfile untouched (placeholder server.mjs stays, T10 wires real start); staged only, no commit (2026-09-20)
- [x] T3.2 auth core — login, opaque sessions, rate-limit/lockout, audit chain — `apps/api/src/lib/audit.ts` (`writeAudit(client,{actorId,action,target,ip,requestId})` hash=SHA256(prev_hash|'|'|ts.toISOString()|'|'|actor|'|'|action|'|'|target) genesis 32 zero bytes, SELECT hash ORDER BY id DESC LIMIT 1, `computeAuditHash` for tests), `apps/api/src/lib/rate-limit.ts` (in-memory per-IP token bucket 5/15min → 429 + Retry-After, <50 LOC, Postgres deferred header), `apps/api/src/lib/auth-middleware.ts` (preHandler `__Host-session` cookie → SHA256 hex → SELECT sessions+users revoked_at IS NULL expires_at>now → 401 or attach user), `apps/api/src/routes/auth.ts` (POST /api/login zod email/password generic 401, per-IP 429, dummy PHC `$argon2id$v=19$m=19456...` for timing equalization on unknown email, locked_until check generic 401, verifyPassword, on fail UPDATE failed_attempts+lockout+15min + audit `auth_lockout` at 5, on success BEGIN reset attempts + 32B random base64url token hash SHA256 + sessions 24h + audit `auth_login` + `__Host-session` HttpOnly Secure SameSite=Strict Path=/; POST /api/logout revoke current + audit `auth_logout`; GET /api/me 401/200; POST /api/sessions/revoke-all revoke all + audit), fixes: `apps/api/src/routes/setup.ts` `name` removed (schema has no column), audit insert replaced with `writeAudit`, verified no `request.body` reaches `request.log` (only `request.log.error(err)`); `apps/api/src/server.ts` wires `registerAuthRoutes`; `apps/api/src/auth.test.ts` 8 tests vitest+Testcontainers (login happy cookie+session hash+expiry 24h +/me, bad pw & unknown email same 401 shape, /me 401/200, logout revokes, revoke-all kills both, 6th rapid 429 Retry-After via per-IP bucket, lockout 5 bad → locked_until+auth_lockout audit+generic 401 while locked, audit chain recompute SHA256(prev|ts|actor|action|target)); TDD RED→GREEN (setup 9 + auth 8 =17/17), `pnpm lint`+`pnpm typecheck` clean strict, `pnpm audit` no new high on api deps; no new deps, no packages/crypto|db src change, no git commit staged only (2026-09-20)
- [x] T3.4 roles + admin-invite + RBAC + IDOR guards — `apps/api/src/lib/roles.ts` (`requireRole('admin'|'user')` preHandler default-deny unknown role → 403, admin rank-2 bypasses all, otherwise role rank must equal/exceed; `requireSelfOrAdmin(paramName='id')` admin bypass + self match, others → 404 `not_found` same shape as missing row, no enumeration), `apps/api/src/routes/invites.ts` (POST /api/invites admin-only zod `{email, role:'user'|'admin'}` → 32 B `randomBytes` base64url token, SHA-256 hex `token_hash` stored, `expires_at` +24 h, `invite_created` audit, 201 `{id, token}` token returned ONCE, never stored plaintext; POST /api/invites/consume public, per-IP rate-limited 5/15 min → 429 + Retry-After, zod `{token,email,password>=12}` (no `name` — removed in T3.2, not reintroduced), single TX `SELECT ... FOR UPDATE` + constant-time `timingSafeEqual` hash compare + `consumed_at IS NULL` + `expires_at > now` + case-insensitive email match, all failures generic 400 `invalid_request` + ROLLBACK, Argon2id `hashPassword`, INSERT users with invited role (23505→400), `consumed_at=clock_timestamp()`, `invite_consumed` audit, COMMIT 201 `{id,email,role}` — single-use atomicity via FOR UPDATE row lock + consumed check in one TX), `apps/api/src/routes/users.ts` (GET /api/users admin-only, SELECT id/email/role/created_at sans `password_hash`; GET /api/users/:id self-or-admin, 404 for others, same shape as unknown id; POST /api/users/:id/revoke-sessions self-or-admin, `revoked_at=clock_timestamp()` on active sessions + `sessions_revoked` audit + clearCookie when self), `apps/api/src/routes/admin.ts` (PUT /api/admin/settings admin-only zod `{openRegistration:boolean}` → upsert `app_config.open_registration` + `admin_settings_updated` audit, same TX), `apps/api/src/routes/register.ts` (POST /api/register gated on `app_config.open_registration`, default 'false' → 404 `not_found`, else zod email/password>=12 → role='user' + `user_registered` audit in TX), `apps/api/src/server.ts` wires `registerInviteRoutes`+`registerUserRoutes`+`registerAdminRoutes`+`registerOpenRegistrationRoute`; `apps/api/src/invites.test.ts` 11 tests Testcontainers `postgres:17-alpine` (non-admin invite 403; invite→consume→login incl. hash-only storage hex64, base64url token shape, audit rows both sides; double-consume 400; expired 400 no user created; cross-user /users/:id 404 vs self 200 vs admin 200 vs unknown-id 404 all same error shape; GET /api/users admin-only sans hashes; revoke-sessions self kills both sessions + admin revokes bob + cross-user 404; register 404 closed → toggle open → 201 role=user → toggle closed → 404; settings toggle admin-only 403 + unauth 401 + audit row + invalid body 400; consume rate-limit 429 + Retry-After; admin-role invite creates admin user); GREEN 28/28 (setup 9 + auth 8 + invites 11), `tsc --noEmit` strict clean, `eslint` clean; no new deps (pnpm-lock untouched), `packages/db` schema/migration untouched (sibling T3.3 owns 0001_totp_webauthn), raw SQL against existing `invites`/`app_config`/`users`/`sessions`; staged only, no commit (2026-09-20)
- [x] T3.3 2FA: TOTP + WebAuthn/passkeys + recovery codes + step-up — `packages/db/drizzle/0001_totp_webauthn.sql` + journal (users: `webauthn_user_id` bytea, `totp_verified_at` timestamptz, `totp_last_counter` bigint default 0; sessions: `totp_pending` bool default false, `webauthn_challenge` text; new tables `recovery_codes(id, user_id fk cascade, code_hash unique, used_at, created_at)` + `webauthn_credentials(id text pk, user_id fk cascade, public_key bytea, counter bigint, transports, device_type, backed_up, created_at)` + GRANTs to role `app`) + `packages/db/src/schema.ts` additive appends (`recoveryCodes`, `webauthnCredentials`, new columns); `apps/api/src/lib/kek.ts` (`MASTER_KEY` env → 32 B KEK, 64-hex or base64, fail-closed at first use, cached + `resetKekCacheForTests`); `apps/api/src/lib/totp-store.ts` (TOTP secret at rest = envelope AEAD ADR-005 XChaCha20-Poly1305 via `@heartbeat-vault/crypto`, kid `totp`, kekVersion 1, AAD `{tenantId:'default',switchId:'totp'}`, canonical-JSON serialization into `totp_secret_encrypted` bytea; `verifyTotpCode` via `otplib@13.5.0` pinned exact: `epochTolerance [30,0]` past-step-only (current+previous, never future) + `afterTimeStep` strict replay rejection, matched step = `epoch/30` stored in `totp_last_counter`); `apps/api/src/routes/totp.ts` (POST /api/2fa/totp/enroll auth-only → `generateSecret` + `generateURI` issuer `Heartbeat Vault`, returns `{otpauthUrl, secret}` ONCE, stores ciphertext + resets `totp_verified_at`/counter + `2fa_totp_enroll_started` audit; POST /api/2fa/totp/verify auth-only, 2fa-scope rate limit, `400 not_enrolled` / `401 invalid_code`, success sets `totp_verified_at` + counter + `2fa_totp_enabled` audit; POST /api/2fa/totp/challenge pending-session-only (`400 totp_not_enabled` precedence, `400 not_pending` for full sessions), flips `sessions.totp_pending=false` in TX with counter update + `2fa_totp_challenge_ok` audit; POST /api/2fa/totp/disable auth-only + zod password + verifyPassword `401 invalid_password`, wipes secret/verified/counter + pending sessions released + recovery codes deleted + `2fa_totp_disabled` audit); `apps/api/src/routes/recovery.ts` (POST /api/2fa/recovery-codes/regenerate auth-only + verified TOTP `400 totp_required` + current-code step-up `401 invalid_code` → 10 codes `XXXX-XXXX` base32 alphabet `A-Z2-7` unbiased byte&31, Argon2id-hashed individually, DELETE-then-INSERT rotation TX + `2fa_recovery_regenerated` audit, plaintext returned ONCE; POST /api/login/recover public, 2fa-scope rate limit, unknown email dummy-Argon2 timing equalization, generic `401 invalid_code` same shape both paths, per-candidate `verifyPassword` over unused hashes, single-use enforced by `UPDATE ... SET used_at WHERE used_at IS NULL` rowCount inside TX → full session (totp_pending=false) + `__Host-session` cookie + `auth_recovery_login` audit, 200 `{id,email,role}`); `apps/api/src/lib/webauthn.ts` (rpID/origin from `WEBAUTHN_RP_ID`/`WEBAUTHN_ORIGIN` env, dev defaults localhost, fail-closed via origin verification; challenge stored per-session `{"challenge","issuedAt"}` JSON with 5-min TTL; transport allow-list runtime-narrowed, no casts); `apps/api/src/routes/webauthn.ts` + `routes/webauthn-register.ts` via `@simplewebauthn/server@13.3.3` pinned exact (POST /api/2fa/webauthn/register-options auth-only, lazily creates 32 B `webauthn_user_id`, `excludeCredentials` existing; register-verify verifies challenge TTL + origin + RPID + UV, upserts credential + `2fa_webauthn_registered` audit, `400 challenge_expired`/`400 verification_failed` fail-closed; login-options pending-only, `400 no_credentials`; login-verify verifies challenge/origin/RPID/counter, TX updates counter + flips `totp_pending=false` + `2fa_webauthn_login_ok` audit); `apps/api/src/lib/step-up.ts` `requireStepUp(pool)` exported (+ re-exported from `routes/two-factor.ts`) for T4 heartbeat/panic: full session + (TOTP verified OR ≥1 WebAuthn credential) else `403 step_up_required`; `apps/api/src/lib/auth-middleware.ts` rejects `totp_pending` sessions default → `403 totp_pending`, opt-in `{allowTotpPending:true}` for 2FA routes only, `AuthUser.sessionTotpPending` added; `apps/api/src/lib/rate-limit.ts` scoped buckets `login` 5/15 min (unchanged behavior) + `2fa` 10/15 min for verify/challenge/disable/regenerate/recover; `apps/api/src/routes/auth.ts` login: TOTP-verified users get `totp_pending=true` session + `202 {stepUp:'totp'}` + `auth_stepup_started` audit, non-TOTP users unchanged 200 (401 shapes untouched); `apps/api/src/server.ts` wires `registerTwoFactorRoutes`; `apps/api/src/two-factor.test.ts` 18 tests Testcontainers `postgres:17-alpine` (enroll returns secret once + only ciphertext at rest >100 B + unverified; verify happy/wrong-401/unenrolled-400 + counter set + audit; enroll→verify→login 202→/me 403 totp_pending→challenge 200→/me 200; wrong challenge 401 stays pending; replay same code 401 on next pending session; challenge 400 totp_not_enabled; 11th challenge 429 + Retry-After; disable wrong password 401 + happy wipes config + login back to 200 + challenge 400; recovery regenerate 10 codes shape + hash-only storage + single-use second-401 + sibling code still works + regenerate invalidates old batch + new batch works; recover unknown email/wrong code same generic shape; regenerate 400 totp_required; webauthn register-options challenge ≥32 b64url + RP + stored in session + pending rejected 403; register-verify garbage attestation 400; login-options 400 no_credentials; login-verify no-challenge 400 challenge_expired; requireStepUp factory; audit rows `2fa_totp_enabled`/`2fa_totp_disabled`/`2fa_totp_enroll_started`); TDD RED→GREEN (RED: missing module; GREEN 18/18), full api suite 46/46 GREEN (setup 9 + auth 8 + invites 11 + two-factor 18), `tsc --noEmit` strict clean, `eslint` clean; `pnpm audit` no new advisories on otplib/simplewebauthn (pre-existing: 5× fastify 5.6.2 GHSA advisories incl. 2 high — see DECISIONS_LOG, bump deferred to dedicated task); unblocks T8.1; staged only, no commit (2026-09-20)

## Phase 4: Switches, heartbeat, trigger engine — DONE (T4.1–T4.6 complete 2026-09-20)

- Modes: asymmetric-key + direct-delivery. Heartbeat options (button, signed email link, API token/webhook, Telegram, TOTP/passkey step-up), interval + escalation (reminders → warning → pending-trigger → final grace → fire), pause/vacation, dry-run. Triggers: missed-heartbeat, fixed datetime, panic, quorum. Pre-fire cancellation window.
- Correctness: downtime compensation + post-recovery grace (fail-safe default, documented), durable crash-safe idempotent multi-instance scheduling (Postgres advisory locks / leader election), no naive wall-clock trust, release-path queue/retry/alert (never silently lost).
- [x] T4.1 switches CRUD + arming guards + recipients + payload storage — `909025a`; 16/16 tests; arm blocked without accepted recipient/payload, fail_deadly typed confirmation, non-owner 404-shape, plaintext-never-in-DB round-trip proof; migration 0002 (title, heartbeat_started_at, next_deadline)
- [x] T4.2 heartbeats: manual / API token / one-time email link + TOTP step-up — `94b7229`; 10/10 tests; deadline reset, hash-only tokens, single-use links (410), TOTP owner forced through step-up; migration 0003 (heartbeat_token_hash, heartbeat_links)
- [x] T4.3 escalation stage ladder (pure) — `29dc10f`; 5/5 tests; normal→reminder→warning(+25%)→pending(+50%)→fire_due→release(48h cancel window), UTC-safe
- [x] T4.4 durable Postgres job queue — `cd673b6`; 9/9 tests; idempotent materialization, FOR UPDATE SKIP LOCKED claim, 30s leases + sweeper, jittered backoff, DLQ at max attempts, dry-run vs multi-recipient fire paths
- [x] T4.5 downtime compensation + clock guard — `ee51c2e`; 8/8 tests; post-recovery grace waits (never fire inside grace), at-most-once backfill, check-in cancels wait, checkClockSkew CLOCK_UNCERTAIN hold; ADR-003 + ADR-004 written
- [x] T4.6 variant triggers + cancellation window — `dbeddb5`; 9/9 tests; fixed_date (future-validated) + panic (typed confirm, fires at fire_at) + quorum (event-driven t-of-n deceased votes, idempotent `switch:{id}:quorum` key); materializer branches heartbeat vs fire_at; 48h pre-fire cancel window via delivery available_at; cancel route (TOTP step-up) aborts pending jobs + future deliveries + disarms; migration 0004 (trigger_type/fire_at/quorum_threshold, recipients.vote, cancelled states). Phase 4 complete: 103/103 api tests.

## Phase 5: Delivery channels — IMPLEMENTATION COMPLETE (commit gate pending)

- Pluggable architecture: SMTP first + webhook + Telegram minimum; retry/backoff, idempotency, receipts, dead-letter queue; recipient pre-approval; per-channel security guidance; payload options (encrypted blob + split key, one-time-view link, direct with explicit risk accept).
- [x] T5.1 durable dispatcher — `apps/api/src/channels/dispatch.ts`; due-job claim with `FOR UPDATE SKIP LOCKED`, provider outcomes persisted as receipt/retry/dead, jittered backoff, configurable per-job retry budget (`0005_delivery_retry_budget.sql`), and delivery DLQ. `dispatch.test.ts`: 8 real-Postgres tests.
- [x] T5.2 SMTP email — `apps/api/src/channels/email.ts`; Nodemailer SMTP adapter, SHA-256-derived stable `Message-ID`, accepted/rejected classification, safe metadata-only release notice, visible dry-run marker. `email.test.ts`: 2 Mailpit-container tests.
- [x] T5.3 webhook + Telegram — `apps/api/src/channels/webhook.ts`, `telegram.ts`; recipient-address webhook endpoint, HMAC-SHA-256 signature plus `Idempotency-Key`, HTTP failure classification; Telegram Bot API response parsing, durable reference text, mockable base URL. `http-channels.test.ts`: 2 local wire-level mock-server tests.
- [x] T5.4 configuration and guidance — `apps/api/src/channels/registry.ts` validates complete SMTP configuration, webhook secret length, and Telegram config through Zod; `.env.example` documents variables; `docs/DELIVERY_CHANNELS.md` documents security and at-least-once semantics. `channel-registry.test.ts`: 3 tests.
- Gate evidence (2026-09-20): focused delivery suite 15/15, complete API suite 118/118, root lint/typecheck/format clean. Commit intentionally pending explicit user authorization.

## Phase 6: Hardened-profile services — ADR-ONLY (T6.1 done 2026-09-20, implementation DEFERRED)

- Vault vs OpenBao ADR + licensing question; standard profile = app + Postgres + proxy only; hardened adds services via Compose profiles; no new SPOF in release path; least-privilege AppRole, short-lived tokens, audit device, Shamir-handled init/unseal. Tests + verify script + installer cover hardened when enabled.
- Gate: ADR-only wave, zero runtime code. No Compose service, no Dockerfile, no Vault/OpenBao client dep.
- [x] T6.1 hardened-profile ADRs + lint guard — `docs/adr/007-hardened-openbao-vs-vault.md` (OpenBao MPL-2.0 candidate default vs Vault CE BSL alternative: Transit key-never-leaves, KV/PKI/dynamic DB creds/audit/seal-unseal, API compat plus token-format caveat, licensing/maturity/operational cost, off-hot-release-path via cached wrapped DEKs plus queue/retry/alert on sealed or unreachable) + `docs/adr/008-caddy-vs-traefik.md` (Caddy 2.11 Alpine candidate: automatic TLS plus `tls internal` LAN plus LE/BYO, Traefik alt for dynamic scale, operational cost); `docs/adr/README.md` index 007/008 Accepted as direction, implementation DEFERRED; `eslint.config.mjs` `no-restricted-imports` guard banning `vault`/`openbao`/`node-vault` project-wide with ADR-007 DEFERRED message, verified by temp import of `vault` (eslint failed) then deleted; grounded in `docs/DESIGN.md` §§1-2, Brief §7.1, and research briefs; explicit DEFERRED markers, no runtime code or deps

## Phase 7: UI — DONE (T7.1–T7.10 complete 2026-09-20; every route exercised by the T8.3 Playwright journey)

- Guided first-run, dashboard (status / next check-in / armed), confirmations on dangerous actions, dark/light, WCAG AA, responsive, on-prem friendly.
- Gate: `pnpm --filter @heartbeat-vault/web build` green + typecheck clean, shell renders without feature pages.
- [x] T7.1 web app shell — Vite 6.4.3 + React 19.3.0 + react-router-dom 7.18.4 + Tailwind 4.3.3 + Zod 4.6.5 pinned exact, `@tailwindcss/vite` 4.3.3 + `@vitejs/plugin-react` 4.7.0, TS strict (`strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`) via `tsconfig.base.json`; `apps/web` layout shell (`Shell` header/nav/main/footer, skip link, `ThemeToggle` class-strategy persisted to `hv-theme`, responsive `max-w-6xl`), placeholder routes `/` `/login` `/setup` `*` 404, `src/lib/api-client.ts` typed fetch wrapper (env `VITE_API_BASE_URL`, `AbortSignal.any` timeout, Zod validation, `ApiError` typed), `src/lib/utils.ts` `cn` + `src/lib/theme.ts`, `components/ui` primitives hand-written (button/card/input/label/dialog) — no `shadcn` CLI, accessible (landmarks, labels, `focus-visible`, `aria-*`); `index.css` design tokens (zinc/emerald, zinc dark, `Geist` sans) — taste `taste-skill` minimalist trust-first, no purple/neon; `pnpm --filter @heartbeat-vault/web build` green (54 modules, 329 kB JS), `typecheck` clean, `lint` clean; `pnpm audit` 2026-09-20 — 0 new high on web deps (vite 6.4.3 patched, pre-existing turbo GHSA-hcf7/GHSA-3qcw + tar-fs/undici via testcontainers noted, not introduced); staged only
- [x] T7.2 guided first-run setup — root `DESIGN.md` establishes the existing neutral/emerald visual system; `apps/web/src/routes/setup.tsx` replaces the placeholder with Zod client validation, password confirmation, typed `POST /api/setup` request, generic safe error states, submitting state, and success transition to login; theme control uses accessible inline SVG + visible label. Verified 2026-09-20: web production build, lint, typecheck, root format check clean; real Playwright Chromium production-preview screenshots at 375px, 768px, and 1280px show no overflow or clipped controls, and full-page mobile verifies footer visibility.
- [x] T7.3 login flow — `apps/web/src/routes/login.tsx` replaces the inert form with typed `POST /api/login`, generic invalid-credential and rate-limit states, disabled submitting state, session redirect, and a TOTP step-up transition; web lint/typecheck/root format clean (2026-09-20).
- [x] T7.4 authenticated dashboard — `apps/web/src/routes/home.tsx` loads `/api/me` and `/api/switches` in parallel with typed Zod schemas, and renders signed-out, loading, error, empty, and switch-status states without dead navigation; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.5a switch creation — `apps/web/src/routes/switch-new.tsx` validates and posts new switches to `/api/switches`; creation remains paused and redirects to the dashboard; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.5b switch detail and management — `apps/web/src/routes/switch-detail.tsx` reads an individual switch, exposes arm/pause controls, and reports arm prerequisites without claiming readiness; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.5c recipients and sealed payload setup — `apps/web/src/components/switches/switch-setup.tsx` creates recipient invitations, shows the returned invitation token only in ephemeral UI state, and submits plaintext to the server sealing endpoint; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.6a manual heartbeat UX — `apps/web/src/components/switches/heartbeat-checkin.tsx` submits an authenticated check-in, supports optional TOTP code, and reports active/step-up outcomes safely; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.6b trigger configuration UX — `apps/web/src/components/switches/trigger-configuration.tsx` configures fixed-date, quorum, and explicitly acknowledged panic triggers through the existing API; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.7 switch edit and deletion UX — `apps/web/src/components/switches/switch-settings.tsx` patches editable switch settings and requires typed-title confirmation before deletion; released switches remain disabled; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.8 administration UX — `apps/web/src/routes/admin.tsx` lists users and creates one-time invitations with ephemeral token display; registration settings intentionally omitted because the API has no read contract; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.9 TOTP login step-up — `apps/web/src/routes/totp-challenge.tsx` completes the previously unregistered `/2fa/totp` login continuation through typed `POST /api/2fa/totp/challenge`; web lint/typecheck/build/root format clean (2026-09-20).
- [x] T7.10 responsive visual QA — CLI Chromium screenshots at 375px, 768px, and 1280px confirmed layout; fixed a 375px header clip by making the brand text visually compact while preserving its accessible name (2026-09-20).

## Phase 8: Tests — DONE (v1 scope, T8.1–T8.5 complete 2026-09-20; SAST/dependency/container/secret scans land with Phase 11 CI; Vault/OpenBao integration deferred with the hardened profile)

- Unit + Testcontainers-Postgres integration, Playwright E2E (bootstrap → login → create/arm → heartbeat → trigger → delivery), crypto KAT/round-trip/negative, failure-injection (crash mid-trigger, duplicate workers, DB restart, clock skew, downtime recovery, Vault sealed/unreachable/restart), Vault/OpenBao container integration, authz/IDOR + rate-limit security tests, SAST/dependency/container/secret scans in CI, coverage thresholds on crypto/auth/scheduler/delivery, Mailpit + mock Telegram/webhook doubles.
- [x] T8.1 web unit-test foundation — `apps/web` now uses pinned Vitest 5 + jsdom + Testing Library; `api-client.test.ts` verifies typed request serialization, typed HTTP errors, and response-schema rejection. `pnpm --filter @heartbeat-vault/web test` 3/3 green; lint/typecheck/build/root format clean (2026-09-20).
- [x] T8.2 repository-wide test baseline — `pnpm test` green: API 118/118, crypto 64/64, DB 7/7, web 3/3. Existing Vitest pool deprecation and Turbo missing-test-output warnings noted; no test failures (2026-09-20).
- [x] T8.3 Playwright end-to-end foundation — `packages/e2e` runs a real-service browser flow with isolated `PostgreSqlContainer('postgres:17-alpine')`, compiled API at `127.0.0.1:3001`, and Vite at `127.0.0.1:5174` proxied through `VITE_API_PROXY_TARGET`. `bootstrap.spec.ts` bootstraps the administrator through `/setup`, follows the real sign-in redirect, creates a default paused switch, creates a recipient and sealed payload through the browser, accepts the recipient token through its public API seam (`POST /api/recipients/accept`), arms, manually checks in and returns to the dashboard to verify the persisted `Active` status and reset `Next check-in` deadline, then configures a panic trigger through the browser (typed confirmation) and polls the real runtime scheduler until the switch reports `Released` on the detail page and dashboard (queued deliveries stay behind the 48 h cancellation window by design). The spec asserts each mutation at the HTTP boundary (recipient/payload 201, check-in 200, trigger 200) before asserting UI state. The trigger leg exposed and closed a runtime gap: `apps/api/src/main.ts` previously started no scheduler, so materialized trigger jobs were never claimed and releases never fired in the real service; it now runs a full cycle per tick (env `TICK_INTERVAL_SEC` default 60, `CLOCK_SKEW_BUDGET_MS` default 5000): ADR-004 clock-skew hold, outage recovery, due-wait promotion, trigger materialization, expired-lease reap, trigger-job claim/process with requeue-or-dead-letter, then delivery dispatch; shutdown awaits the in-flight tick before pool teardown. The E2E harness runs the API with `TICK_INTERVAL_SEC=1`, starts Mailpit (`axllent/mailpit:v1.27.4`, SMTP 1025 / HTTP 8025), and configures the API's email channel via `SMTP_HOST`/`SMTP_PORT`/`SMTP_FROM`. It also locks regressions in `SwitchSetup` and `HeartbeatCheckin`: form elements are retained before asynchronous requests, preventing successful submissions from being falsely reported as failures after React clears `event.currentTarget`. The spec then seeds one due `delivery_jobs` row for the released switch (test seam bypassing the cancellation window), waits for the runtime dispatcher to mark it `succeeded`, and verifies the Mailpit message: recipient address, `Heartbeat Vault release ready` subject, the idempotency-derived `Message-ID` (RFC 5322 header unfolding applied), metadata-only body, and no `[DRY RUN]` marker. The harness builds crypto/DB/API before Playwright and always stops its processes/container. Verified 2026-09-20: `pnpm --filter @heartbeat-vault/e2e test` 1/1 (bootstrap → login → create → invite → seal → accept → arm → check-in → panic → scheduler-released → dispatched-to-Mailpit), E2E lint/typecheck, and the full `pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build` gate green (API 118/118, crypto 64/64, DB 7/7, web 3/3, E2E 1/1). Generated Playwright reports are git- and formatter-ignored.
- [x] T8.4 API coverage gates on auth/scheduler/delivery — `apps/api` now pins `@vitest/coverage-v8@5.0.1` (exact, matching crypto) with a `test:coverage` script; the turbo `test:coverage` task was already wired. `apps/api/vitest.config.ts` scopes v8 coverage `include` to the Brief's gated areas — auth (`src/routes/auth.ts`, `src/routes/two-factor.ts`, `src/lib/auth-middleware.ts`, `src/lib/totp-store.ts`), scheduler (`src/lib/trigger-engine.ts`, `src/lib/downtime.ts`, `src/lib/escalation.ts`), and delivery (`src/channels/**`) — with enforced thresholds at ~95%-of-measured baseline: lines 84, functions 88, branches 65, statements 84. Measured baseline 2026-09-20: 87.43% lines, 87.46% statements, 92.72% functions, 70.58% branches (weakest files: `webhook.ts` 46% stmts, `telegram.ts` 60% stmts). Runtime-verified reporter behavior: the v8 text table omits 100%-covered files while still counting them in the aggregate, so a hidden file regressing below 100% re-appears and drops the total — the threshold gate still catches it. `pnpm --filter @heartbeat-vault/api test:coverage` green (14 files / 118 tests, thresholds enforced).
- [x] T8.5 failure-injection gap closure: DB restart durability — `trigger-engine.test.ts` now proves the release path survives a real database crash: a claimed trigger job is committed, the Postgres container is SIGKILLed via docker CLI (`docker kill` = true crash; testcontainers `restart()` was evaluated and rejected because its stop step removes the container) and restarted (entrypoint replays WAL), then a fresh pool — connected through the rediscovered port mapping, since Docker reassigns random host ports on restart — verifies the claim is still `running`/owned by `w1`, the lease sweeper reaps it, and another worker re-claims and completes it. The old pool is closed before the kill so no client observes the crash. Existing failure-injection coverage was verified before writing: lease-expiry sweep, duplicate-worker claim exclusivity, clock-skew budget hold, downtime recovery with post-recovery grace, and trigger/delivery retry + dead-letter paths were already tested; DB restart was the one uncovered failure mode (Vault scenarios deferred with the hardened profile). `pnpm --filter @heartbeat-vault/api exec vitest run src/trigger-engine.test.ts` 10/10 green (API suite total now 119/119).

## Phase 9: Verification script — DONE (T9.1 complete 2026-09-20)

- `scripts/verify-security.sh` (+ make/CLI target): read-only, non-destructive, never prints secrets; container hardening, TLS/headers, DB exposure + least-privilege roles, file perms + no default/weak creds, encryption-at-rest proof, crypto self-test, image digest/checksum/signature, bootstrap disabled, backup encryption, hardened-profile checks; human PASS/WARN/FAIL + JSON + exit codes.
- [x] T9.1 verify-security.sh — `scripts/verify-security.sh` implements BRIEF §10 as 18 read-only, non-destructive checks (21 emitted results in dev): image digest pinning (multi-stage-FROM-aware), db not exposed (comment-safe awk), loopback-only published ports (base + dev override), Caddy admin never published, API non-root, .env git-ignored/untracked + file perms, credential hygiene (placeholder/short detection, environment-driven WARN in dev vs FAIL in production, values never printed), TLS + security headers, prod restart/limit overrides, static sealing proof (encrypt before INSERT), runtime encryption-at-rest + least-privilege role proof when the stack is up, bootstrap-state probe (non-destructive POST with empty body: 400 open / 410 disabled), crypto self-test via the pinned crypto suite, backup-encryption and hardened-profile WARNs. Output: human PASS/WARN/FAIL report or `--json` (Node-parsed valid); exit codes 0 (no FAIL) / 1 (any FAIL — proven live by planting an unpinned image) / 2 (usage). Exposed as CLI: `pnpm verify:security`. First real run surfaced a genuine finding: the running T1.3-era dev stack serves a placeholder API (POST /api/setup → 404; unmigrated db schema) — flagged as WARN "stale deployment" as designed. bash -n clean (shellcheck not installed). Run evidence: 14 PASS / 7 WARN / 0 FAIL, exit 0 in development.

## Phase 10: Installer and deployment — DONE (T10.1–T10.2 complete 2026-09-20)

- [x] T10.1 install.sh — one-command idempotent installer: preflights (docker/compose v2, port-in-use + memory WARNs), openssl-hex secret generation (URL-safe for DATABASE_URL interpolation; placeholder values upgraded in-place, real values never overwritten or printed), .env created from .env.example at mode 600, profile-aware compose up (dev base+override; `--prod` base+prod), health wait (120s via loopback /api/health), then prints the first-run setup URL + one-time token (sha256 hash upserted into app_config with 24h expiry; shown once). Hardened profile rejected with ADR-007 rationale (deferred in v1). TLS: `internal` wired (Caddy internal CA); `le`/`byo` print exact manual steps (static Caddyfile in v1). Fixed the compose api healthcheck path (`/health` → `/api/health` — the real Fastify route).
- [x] T10.2 companion subcommands — `status` (compose ps + health probe), `backup` (pg_dump | gzip into git-ignored `backups/`, optional BACKUP_ENCRYPTION_KEY encryption), `restore` (decrypt + gunzip + psql, destructive `--yes` gate), `uninstall` (`down`; `--volumes` opt-in data deletion, `--yes` gate), `upgrade` (rebuild + up — migrations run via the container entrypoint). Real-run evidence (dev): install exit 0 — db healthy, api healthy, migrations applied (17 public tables), one-time token persisted (app_config `setup_token_hash` non-empty, `setup_completed=false`), scheduler clean (0 tick failures), setup probe 400 (open), backup file created, idempotent re-run preserved secrets (hash-verified) and reissued the token. The installer's debugging fixed two real product bugs: (1) base64 secrets broke DATABASE_URL URL interpolation (`Invalid URL` at startup) → hex generation + updated .env.example guidance; (2) migrate/seed CLI entries silently no-oped in containers — pnpm symlinks workspace deps so `argv[1]` ≠ realpath of `import.meta.url` → symlink-robust `realpathSync` comparison in both CLIs (plus a missing seed.ts import caught by typecheck). Verify-security cross-check after install: 16 PASS / 3 WARN / 0 FAIL (bootstrap open 400, no payloads yet, backup key unset — all expected fresh-install states).

- Idempotent `install.sh`: preflights, secure secret generation, restrictive-perm config, Compose up, healthcheck wait, prints setup URL + one-time token; standard/hardened choice; safe re-run; host untouched beyond Docker + project dir; TLS options (self-signed/internal CA, Let's Encrypt, BYO); upgrade/backup/restore/status/uninstall; healthchecks, restart policies, HA/scale-out docs.

## Phase 11: CI/CD and versioning — DONE (T11.1–T11.4 complete 2026-09-20; pipelines verified locally — YAML-parsed and the exact CI commands run green; live Actions runs require pushing the repository to GitHub, which has no remote configured yet)

- Git tag semver (Conventional Commits + release-please/semantic-release), CHANGELOG.md, signed multi-arch images + SBOMs; gates (lint, typecheck, tests, scans, build); default GitHub Actions + GHCR (pending Phase 0 answer); secrets/settings documented, no fabricated results.
- [x] T11.1 CI gate — `.github/workflows/ci.yml` on push/PR (ubuntu-latest, explicit least-privilege `permissions:`): frozen-lockfile install, Playwright chromium, format:check, lint, typecheck, build, full tests (unit + integration + crypto + E2E on Docker), `pnpm audit --audit-level=low` gate, plus a gitleaks secret-scanning job over full history.
- [x] T11.2 SAST — `.github/workflows/codeql.yml`: CodeQL javascript-typescript on push/PR + weekly schedule; results to the Security tab.
- [x] T11.3 versioning — `.github/workflows/release-please.yml`: googleapis/release-please-action@v4 (node type) maintains CHANGELOG.md and version bumps via release PRs from Conventional Commits; merging creates the vX.Y.Z tag.
- [x] T11.4 release images — `.github/workflows/release.yml` on `v*` tags: full gate re-run, then docker buildx multi-arch (linux/amd64 + linux/arm64) push to GHCR (built-in GITHUB_TOKEN, `packages: write`), trivy scan failing the release on CRITICAL/HIGH, SPDX SBOM (syft) uploaded as an artifact, cosign keyless signing via OIDC (`id-token: write` — no stored secrets). Explicit per-workflow permissions everywhere.
- [x] Dependency-audit gate — root `pnpm.auditConfig.ignoreGhsas` registers the 18 dev-only advisories (undici 12 / tar-fs 3 / turbo 2 / esbuild 1 — via testcontainers/vite/turbo, not shipped in the prod image) so `pnpm audit --audit-level=low` fails on any NEW advisory; the 15 production advisories (fastify 5, nodemailer 10 — including 4 highs on fastify/nodemailer) were RESOLVED by upgrading fastify 5.6.2→5.12.5 and nodemailer 7.0.11→10.0.10, validated by the full 119-test API suite + the E2E Mailpit delivery journey (10.4s green). AUDIT_EXIT=0.
- Verification honesty: YAML parsed (python3), workflows prettier-formatted, and the exact CI commands were executed locally in CI order — all green (frozen-lockfile install, format/lint/typecheck/build/test/audit). The actual GitHub Actions execution could NOT be verified from this environment: the repository has no git remote yet. After the first push: enable branch protection on main requiring CI + CodeQL; SHA-pinning of actions is a recommended supply-chain hardening follow-up. Required GitHub settings: none beyond the explicit per-workflow permissions (GHCR package auto-created on first publish — set visibility; gitleaks free for public repos; cosign keyless uses OIDC).

## Phase 12: Documentation — DONE (T12.1–T12.4 complete 2026-09-20; documentation claims trace to implementation, configuration, tests, or explicit verification limits)

- [x] T12.1 User README — `README.md`: purpose, lifecycle, plain-language fail-safe semantics, Mermaid diagrams, quickstart/bootstrap, configuration and profile boundaries, heartbeat/recipient/channel guidance, key handling, backup/restore/upgrade, verification, FAQ, and responsible-use/legal limitations.
- [x] T12.2 Architecture and operations — `docs/ARCHITECTURE.md` and `docs/OPERATIONS.md`: standard Compose topology, package boundaries, encrypted storage and durable scheduler flow, installer/status/backup/restore/upgrade runbooks, TLS/profile limits, and production checklist.
- [x] T12.3 Security documentation — `docs/SECURITY.md` and `docs/THREAT_MODEL.md`: end-to-end ciphertext-at-rest design, authentication/authorization/release controls, STRIDE mapping, explicit server-compromise boundary, verification/incident guidance, and residual risks.
- [x] T12.4 Contributor and API reference — `CONTRIBUTING.md` and `docs/API.md`: development/security/test expectations and all registered v1 route groups (including token, account/admin, 2FA, WebAuthn, and switch lifecycle endpoints).
- Documentation verification: `pnpm format:check` passed after all documentation edits. This phase documents the shipped standard profile only; OpenBao/Vault remains ADR-backed and deferred, and actual GitHub Actions execution remains unverified until a remote is configured and workflows are pushed.

## Final Phase: Recursive self-audit — DONE (2026-09-20; documentation/API/verifier audit completed, Oracle review findings corrected, and final local gate green)

- [x] Traceability: README, architecture, security, threat model, operations, API, contribution guide, delivery-channel guidance, and ADRs cover the standard v1 implementation; docs explicitly state the server-side KEK boundary and fail-safe scheduler semantics.
- [x] API accuracy: the final Oracle audit caught and corrected documented-but-unregistered user/admin methods; `docs/API.md` now reflects `GET /api/users/:id` and `PUT /api/admin/settings`, not invented create/update/read methods.
- [x] Verifier accuracy: the final audit caught and corrected false hardened-profile detection from compose comments; `scripts/verify-security.sh` now checks only service declarations within the Compose `services:` map and reports deferred v1 hardening as WARN.
- [x] Final local evidence: `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm test` (119 API, 64 crypto, 7 DB, 3 web, 1 E2E), and `pnpm audit --audit-level=low` all passed; `bash -n scripts/verify-security.sh` passed; security verification reported 15 PASS / 4 WARN / 0 FAIL.
- [!] Verification limits: GitHub Actions/release/GHCR/cosign cannot run until a remote is configured and workflows are pushed; the hardened Vault/OpenBao profile is intentionally deferred; expected standard-install WARNs remain for open bootstrap, no stored ciphertext payload, and unset backup encryption key.

## Tailnet bind-IP — DONE (2026-09-21; CADDY_BIND_IP Tailnet-only publish)

- [x] Caddy `80`/`443` host bindings now use `${CADDY_BIND_IP:-127.0.0.1}` in `docker-compose.yml`; default remains loopback. `.env.example` documents `CADDY_BIND_IP=127.0.0.1` with Tailnet guidance (`100.124.184.116` + `HTTP_PORT=80`/`HTTPS_PORT=443` + `APP_URL`).
- [x] `install.sh` probes the configured bind IP first (`https://$CADDY_BIND_IP:${HTTPS_PORT}` / `http://$CADDY_BIND_IP:${HTTP_PORT}`) before loopback; `wait_healthy` and `status` both respect the setting.
- [x] `scripts/verify-security.sh` now validates `CADDY_BIND_IP` (FAIL on wildcard/CIDR/invalid), restricts published ports so only the exact Caddy `80`/`443` mapping for the configured IP passes (service-scoped parser, WARN upgraded to FAIL for other services/ports), probes the Tailnet base for bootstrap, and adds WARN-only runtime port evidence. New ids `caddy-bind-ip` and `ports-runtime` appear in reports.
- [x] Docs trace to implementation: `README.md` (default+Tailnet paragraph, `CADDY_BIND_IP` table row, FAQ wildcard note), `docs/ARCHITECTURE.md` (loopback default + single-IP rebind), `docs/OPERATIONS.md` (Tailnet operation section incl. `__Host-`/HTTPS caveat, `ss`/`curl`/smoke checks, peer manual step), `docs/SECURITY.md` (exact-IP verifier guarantee).
- [!] Verification limit: `tailscale` CLI unavailable on this host — binding proven host-locally (`ss`/`docker compose port`/`curl` to `100.124.184.116`); reachability from another Tailnet device is a documented manual check.

## Wave 2a: Switch read metadata and dashboard filters — DONE (2026-09-26)

- [x] Switch list and detail reads serialize ISO 8601 `createdAt` and `updatedAt`; `ownerEmail` is present only for an administrator's `GET /api/switches?all=1` items and omitted for all detail and non-administrator reads.
- [x] The dashboard loads the all-switch list once, derives case-insensitive title and status filters locally, presents a neutral zero-match state, formats timestamps, and renders owner metadata only for administrators. Detail settings updates retain read metadata.
- [x] `docs/API.md` records the response fields and owner-email authorization boundary. Gate evidence: `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` passed (API 144, web 59, crypto 64, DB 13, E2E 1); production-preview Playwright checks passed at 1280px, 768px, and 375px with no filter-triggered requests or console errors; visual review PASS.

## Key decisions

- 2026-09-20 (user answers): deployment = single-household, one host, documented HA path; channels v1 = Email + Webhook + Telegram (Matrix/SFTP deferred); CI = GitHub Actions + GHCR; UI/docs = English only; license = permissive MIT/Apache-2.0 family (exact pick pending); threat priority = stolen-data-at-rest first (full STRIDE + ASVS map still required); release model = server-side automatic release for v1; hardened profile = NOT in v1 — standard-only v1, hardened (Vault vs OpenBao) deferred to next-development todo with ADRs.

## Open issues

- The Phase 0 design document retains its historical `PROPOSED` label and its explicit approval was not separately recorded before subsequent directed implementation. The user later directed autonomous completion; this tracker does not fabricate a historical approval event.
- GitHub Actions/release/GHCR execution and hardened-profile behavior remain externally unverified for the reasons recorded above.
