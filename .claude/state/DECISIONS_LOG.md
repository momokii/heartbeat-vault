## Decisions Log

> No decisions logged yet. This file must be updated by the agent whenever a
> significant decision is made during a working session.

### Template Format

---

**Decision:** [What was decided]
**Date:** [YYYY-MM-DD]
**Context:** [Why this decision was needed]
**Rationale:** [Why this option was chosen]
**Alternatives Rejected:** [Other options considered and why they were not chosen]
**Security Implications:** [Any security impact of this decision]
**Impact:** [What this decision affects downstream]

---

### Entries

- 2026-09-20 — **Decision:** T2.1 DB schema + migrations — `drizzle-orm@0.45.2` + `pg@8.13.3` pinned exact, `drizzle-kit@0.31.4` + `testcontainers@10.14.0` + `@testcontainers/postgresql@10.14.0` + `tsx@4.19.1` + `vitest@5.0.1` pinned exact; 14 tables per DESIGN.md §3 via `packages/db/src/schema.ts` (timestamptz `clock_timestamp()`, bytea customType, interval, checks, FK cascades, UNIQUEs), `drizzle/0000_brown_titania.sql` with `CREATE EXTENSION pgcrypto` + roles `migrator`/`app` + least-privilege GRANTs + `REVOKE UPDATE,DELETE ON audit_log FROM app` + default privileges, `src/migrate.ts` drizzle-orm migrator runner, `src/seed.ts` idempotent `app_config` defaults only (ON CONFLICT DO NOTHING), `test/helpers.ts` PostgreSqlContainer `postgres:17-alpine` pinned + `createTestDb`/`getDb`/`truncateAll`, `src/schema.test.ts` 7 tests green on real Postgres (tables exist, UNIQUE(switch_id,deadline_at), app RBAC via app_tester, timestamptz UTC, singleton, sealed_payloads unique, idempotency_key unique); TDD RED→GREEN, strict TS no `as any`, single-quote 100-width 2-space (`.prettierrc.json` singleQuote→true), `pnpm --filter @heartbeat-vault/db typecheck` + `lint` clean.
  **Date:** 2026-09-20
  **Context:** Plan Wave 3 T2.1 — DB foundation per DESIGN.md §3; depends on T1.1+T1.3 (done), blocks T3.1/T4.1/T5.1; siblings T2.3/T2.4/T6.1/T7.1 parallel — must not touch `packages/crypto/src`, `apps/*`, `docs/adr/`, compose/Caddyfile/eslint configs except `pnpm-lock.yaml`+`PROGRESS`+`DECISIONS_LOG`+`.prettierrc.json` style fix.
  **Rationale:** `drizzle-orm` code-first + `drizzle-kit` generate gives type-safe schema + SQL migration checked into `drizzle/` + `migrate.ts` runner for Testcontainers and compose; `pg` driver is standard node-postgres; `testcontainers` 10.14.0 with `postgres:17-alpine` matches compose `db` image tag (pinned digest in compose, tag pinned in testcontainers for ephemeral DB); `@testcontainers/postgresql` module provides `PostgreSqlContainer` (core `testcontainers` 10.x no longer exports it); `tsx` for `db:migrate`/`db:seed` scripts; `clock_timestamp()` for wall-clock UTC per DESIGN.md §3; bytea via `customType` with `Buffer`; roles `migrator` (DDL) vs `app` (DML, no UPDATE/DELETE on audit_log) enforced in migration (least-privilege, default privileges).
  **Alternatives Rejected:** Prisma (heavier, not Drizzle per plan), `knex`/`typeorm` (less type-safe), `postgres` (pg) vs `pg` driver (plan specifies `pg`), `GenericContainer` manual postgres (less ergonomic than `PostgreSqlContainer`), `pg-mem` (not real Postgres — fails to catch real FK/interval/bytea/privilege behavior), `vitest` `pool: threads` (flaky with testcontainers — use `forks:singleFork`).
  **Security Implications:** `pnpm audit` 2026-09-20: 19 vulns — 6 high (`tar-fs` symlink/path traversal + `undici` WebSocket unbounded memory/exception — all via `testcontainers`/`tar-fs` dev-only), 10 moderate (`esbuild` dev-server + `undici` decompression/smuggling/CRLF/header-injection + `turbo` CSRF), 3 low (`turbo` local-exec + `undici` cookie/SameSite/queue). Production deps `drizzle-orm@0.45.2` + `pg@8.13.3` have 0 high; highs are `tar-fs`/`undici`/`esbuild` in dev-only `testcontainers`/`vitest`/`turbo` — not shipped to prod image (prod image only has `drizzle-orm`+`pg` runtime). `audit_log` REVOKE verified via non-superuser `app_tester` (superuser SET ROLE bypasses checks — use login tester). Pre-existing `turbo@2.5.4` GHSA-hcf7-66rw-9f5r moderate + GHSA-3qcw-2rhx-2726 low (≤2.9.13, patched ≥2.9.14) tracked. No `as any`/`@ts-ignore`; migration `REVOKE UPDATE,DELETE` green.
  **Impact:** `packages/db` (`package.json`, `drizzle.config.ts`, `src/schema.ts`, `src/migrate.ts`, `src/seed.ts`, `src/index.ts`, `test/helpers.ts`, `src/schema.test.ts`, `vitest.config.ts`, `drizzle/0000_brown_titania.sql`+meta, `.prettierrc.json` singleQuote true), `docs/PROGRESS.md` T2.1 marked done, `pnpm-lock.yaml` updated; staged only, no commit; unblocks T3.1/T4.1/T5.1.

- 2026-09-20 — **Decision:** T7.1 web shell — Vite 6.4.3 + React 19.3.0 + react-router-dom 7.18.4 + Tailwind 4.3.3 + Zod 4.6.5 pinned exact (deps `react`/`react-dom`/`react-router-dom`/`zod`, dev `@tailwindcss/vite` 4.3.3 + `@vitejs/plugin-react` 4.7.0 + `tailwindcss` 4.3.3 + `vite` 6.4.3 + `@types/react` 19.3.0 + `@types/react-dom` 19.3.0 + `@types/node` 22.13.10 + `typescript` 5.7.3); taste `taste-skill` minimalist trust-first (zinc/emerald, `Geist` sans, no purple/neon, `min-h-[100dvh]`, `focus-visible:ring`, no emoji icons).
  **Date:** 2026-09-20
  **Context:** Plan Wave 3 T7.1 — shell per DESIGN.md §5/7; depends on T1.1/T1.2 (done), blocks T7.2-7.5; siblings T2.3/T2.4/T6.1 done separately — must not touch their files (`packages/crypto/src`, `docs/adr/`, compose) nor feature pages (wizard/dashboard/switch/admin deferred to T7.2-7.5).
  **Rationale:** Vite 6.4.3 patched (`server.fs.deny` bypass fixed ≤6.4.2 → 6.4.3) + `@vitejs/plugin-react` 4.7.0 stable for Vite 6; Tailwind 4.3.3 via `@tailwindcss/vite` (Vite plugin, not postcss) with `@theme` CSS variables + `@custom-variant dark` class strategy; React 19.3.0 + Router 7.18.4 pinned exact per SECURITY_STANDARDS; Zod 4.6.5 for `api-client.ts` placeholder validation; hand-written `components/ui` (button/card/input/label/dialog) — no `shadcn` CLI dep, accessible; `tsconfig.base.json` strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` + `verbatimModuleSyntax`; build `vite build` only.
  **Alternatives Rejected:** Vite 8.3.0 (`@vitejs/plugin-react` 6.1.1 `vite/internal` import fails on Node 22 — rolled to 6.4.3), Vite 7.1.x (same compat), `shadcn` CLI (extra dep, not needed for shell), `shadcn/ui` install (would pull Radix + Tailwind 3 compat), custom purple gradient (banned per taste rules), `h-screen` (iOS jump — use `min-h-[100dvh]`), emoji icons (banned — use text glyphs).
  **Security Implications:** `pnpm audit` 2026-09-20: 0 new high on web deps (vite 6.4.3 patched for GHSA-xxx `server.fs.deny`; react/zod/tailwind clean); remaining 6 high/10 moderate/3 low are pre-existing `tar-fs`/`undici`/`esbuild` via `testcontainers`/`tailwind`/`turbo` (not introduced by T7.1, tracked); `api-client.ts` typed fetch + Zod safeParse, `AbortSignal.any` timeout, no auth/token, base URL from `VITE_API_BASE_URL` (env only); theme persisted to `localStorage` only, inline `<script>` no external fetch; `pnpm --filter @heartbeat-vault/web build` green (54 modules), `typecheck` clean (strict), `lint` clean; no `as any`/`@ts-ignore`.
  **Impact:** `apps/web` shell: `package.json`, `tsconfig.json`, `tsconfig.node.json`, `vite.config.ts`, `index.html`, `src/index.css`, `src/vite-env.d.ts`, `src/main.tsx`, `src/App.tsx`, `src/lib/{api-client,utils,theme}.ts`, `src/components/ui/{button,card,input,label,dialog}.tsx`, `src/components/layout/{shell,header,theme-toggle}.tsx`, `src/routes/{home,login,setup,not-found}.tsx`; `docs/PROGRESS.md` T7.1 marked done; `pnpm-lock.yaml` updated; staged only, no commit.

- 2026-09-20 — **Decision:** T2.3 Argon2id KDF + KEK rotation — `@node-rs/argon2@2.2.1` pinned exact (napi-rs, RustCrypto Argon2 0.5, OWASP `m=19456,t=2,p=1`, 16 B salt, 32 B output, PHC `$argon2id$v=19$`, `secret` pepper via `KEK_PEPPER` env, `hash`/`verify`/`hashRawSync`/`parseOptions`) for `hashPassword`/`verifyPassword`/`deriveKEK`/`deriveKEKAsync`/`needsRehash` (fail-closed `requirePepper` option, never fallback pepper, malformed PHC throws) + `rotation.ts` pure helpers `rotateKEK` (decrypt DEK with old KEK + old `kekVersion` AAD, wrap with new KEK + `newVersion` AAD, new 24 B nonce, bump `kekVersion`, keep `kid`/`payload`, idempotent, batch `Envelope[] → Envelope[]`, AAD-resolver optional, `tryUnwrapDEK` candidate fallback, `memzero`) / `needsRotation` (`kekVersion !== currentVersion`); strict TS (no `as any`, verbatimModuleSyntax workaround via local `Algorithm`/`Version` const), `envelope.ts` read-only.
  **Date:** 2026-09-20
  **Context:** Plan Wave 3 T2.3 — KDF + rotation per DESIGN.md §4; depends on T2.2 (done), blocks T3.1/T3.2; sibling T2.4 done separately (not touched), T6.1 ADRs done. Must not touch DB/API/`docs/adr/`/compose.
  **Rationale:** `@node-rs/argon2` is the only maintained napi-rs binding with prebuilt binaries (no node-gyp), first-class PHC + `needsRehash` via `parseOptions`, `secret` pepper, `hashRaw` for KEK derivation; OWASP 19456/2/1 is the 19 MiB baseline for single-thread server KDF; pepper fail-closed prevents silent downgrade to pepperless hash; `deriveKEK` sync (`hashRawSync`) keeps batch derive compatible with `await` and sync callers, `deriveKEKAsync` offered for async callers; rotation pure + `SKIP LOCKED` sweep deferred to T4 keeps release path DB-only and idempotent (double-rotate no-op, old KEK retained decrypts old envelope).
  **Alternatives Rejected:** `argon2` (native, node-gyp, no `parseOptions`/`hashRawSync`), `hash-wasm` (WASM, slower, no `secret` pepper), `scrypt` fallback (not Argon2id), `bcrypt` (not memory-hard), custom salt/pepper concatenation (non-standard, no `secret` parameter), fallback default pepper (rejected per spec — fail-closed only).
  **Security Implications:** Audited lib only (`@node-rs/argon2` + `@noble/ciphers`); no custom crypto; PHC verification constant-time via argon2; `needsRehash` enforces upgrade on old `m/t/p` or short salt; pepper changes KEK output, missing pepper with `requirePepper` throws; `rotateKEK` zeroizes DEK after re-wrap, new nonce per re-wrap, old envelope still decrypts with old KEK (key retention), new envelope only with new KEK; `pnpm audit` 2026-09-20: no new advisory for `@node-rs/argon2@2.2.1`; pre-existing: `turbo@2.5.4` GHSA-hcf7-66rw-9f5r + GHSA-3qcw-2rhx-2726 (≤2.9.13, patched ≥2.9.14) + `undici` via `testcontainers`/`tailwind` (not introduced by T2.3); `pnpm --filter @heartbeat-vault/crypto typecheck` clean; TDD RED→GREEN 21 passed.
  **Impact:** `packages/crypto/src/kdf.ts`, `kdf.test.ts` (7 tests), `rotation.ts`, `rotation.test.ts` (5 tests), `src/index.ts` (new exports), `package.json` (`@node-rs/argon2@2.2.1` pinned), `DESIGN-NOTE.md` ADR-005 extended + RED/GREEN, `docs/PROGRESS.md` T2.3 marked done.

- 2026-09-20 — **Decision:** T1.2 lint/typecheck/format/hooks — ESLint 9.39.5 + @eslint/js 9.39.5 + typescript-eslint 8.70.0 + globals 16.5.0 + eslint-config-prettier 10.1.8 + prettier 3.9.8 + lint-staged 17.5.1 + husky 9.1.7 pinned exact; flat `eslint.config.mjs` (recommended + ban `any`/`@ts-ignore`) + `.prettierrc.json` (100 width, semi, trailingComma all) + `.editorconfig` + `.prettierignore` + lint-staged (eslint --fix + prettier) + Husky `.husky/pre-commit` (`pnpm exec lint-staged`); `pnpm lint` / `typecheck` / `format:check` wired; per-package `lint` → `eslint .`.
  **Date:** 2026-09-20
  **Context:** Plan Wave 2 T1.2 — tooling foundation blocks T6.1, T7.1, T11.1; depends on T1.1. Parallel siblings T1.3 (Docker), T1.4 (ADRs), T2.2 (crypto) — must not touch their files.
  **Rationale:** ESLint flat config is the only supported config as of 9.x (eslintrc deprecated); typescript-eslint 8.70.0 matches eslint 9.39.5 maintenance channel; prettier 3.9.8 is current stable; lint-staged + Husky is the pnpm-native pre-commit path. Exact pins per SECURITY_STANDARDS (no ^ ranges).
  **Alternatives Rejected:** Biome (would replace both but violates ADR-001 ESLint choice and 2 siblings expect ESLint); XO/Standard (opinionated, less TS control); `eslint 10.x` (latest) — typescript-eslint 8.70.0 caps at eslint 9, so pinned to 9.39.5 maintenance line.
  **Security Implications:** `pnpm audit` 2026-09-20: 0 advisories on new deps (eslint, typescript-eslint, prettier, eslint-config-prettier, globals, husky, lint-staged clean); 2 pre-existing advisories on `turbo@2.5.4` (GHSA-hcf7-66rw-9f5r moderate + GHSA-3qcw-2rhx-2726 low, patched ≥2.9.14) — not introduced by T1.2, tracked for follow-up; no new attack surface, hooks run locally only. No `as any`/`@ts-ignore` allowed via eslint rule.
  **Impact:** `eslint.config.mjs`, `.prettierrc.json`, `.prettierignore`, `.editorconfig`, `.husky/pre-commit`, root + 6 workspace `package.json` scripts/devDeps, `turbo.json` (unchanged but wired), `.claude/ENVIRONMENT_GUIDE.md`, `docs/PROGRESS.md`; `pnpm lint`/`typecheck`/`format:check` pass; pre-commit enforces lint-staged; sibling files (`docs/adr/`, `packages/crypto/src`, compose/Dockerfile) excluded from format via `.prettierignore` to avoid parallel conflicts.

- 2026-09-20 — **Decision:** T2.4 asymmetric sealed-box + Shamir sharing — `libsodium-wrappers@0.8.4` (pinned exact, 2026-04-19, 2.3M/wk, ISC, sig `SHA256:DhQ8wR5APBvFHLF/+Tc+AYvPOdTpcIDqOhxsBHRwC7U`, libsodium 1.0.18 C core audited) for `crypto_box_seal`/`crypto_box_seal_open`/`crypto_box_keypair` (X25519 → HKDF-SHA256 → ChaCha20-Poly1305, 48B overhead, anonymous, 32-byte keys) + `shamir-secret-sharing@0.0.4` (pinned exact, Apache-2.0, 2025-01-10, sig `SHA256:jl3bwswu80PjjokCgh0o2w5c2U4LhQAE57gj9cz1kzA`, zero-dep TS) audited by **Cure53 Jan 2023 PVY-01 (2 senior testers)** + Zellic (1 high fixed, 2 info LUT side-channel, "well-written") + `@noble/hashes@1.8.0` (pinned exact, BLAKE2b-512) for honest-dealer `BLAKE2b(secret)` commitment + `verifyCommitment` constant-time `equalBytes`; strict TS (`no as any`, `noUncheckedIndexedAccess`), validation `2<=t<=n<=255`; v1 server-side custody, recipient-shares deferred (documented in `DESIGN-NOTE.md` custody table).
  **Date:** 2026-09-20
  **Context:** Plan Wave 3 T2.4 — Type 1 asymmetric-key mode (Brief §4) + DESIGN.md §4 + research `crypto-primitives-2026.md` §§2.2/2.6/6/9. Depends on T2.2 (done), blocks T2.5. Must not touch `envelope.ts`, DB/API, or `docs/adr/`.
  **Rationale:** libsodium sealed-box is the audited NaCl ceiling for anonymous single-recipient blobs (48B overhead proven); shamir-secret-sharing is the only audited Shamir TS lib (Cure53+Zellic, GF(2⁸), vault-inspired, t-of-n); BLAKE2b commitment gives corrupt-share detection without VSS. All pinned exact per SECURITY_STANDARDS, `pnpm audit` clean on these deps (new advisories only on pre-existing `turbo`/`vite`/`tar-fs`/`undici` via `testcontainers`/`tailwind`, not on crypto deps).
  **Alternatives Rejected:** `age-encryption` (multi-recipient file envelope, heavier, not needed for single-blob v1 — keep as alt in DESIGN-NOTE), `sodium-plus` (stale 2020-07, 21 deps, wraps same libsodium), `shamir-secret-sharing-extended` (no audit, couples AES-GCM per-share unnecessarily), custom XOR threshold (no math).
  **Security Implications:** Secrets never logged; private keys validated 32B; tampered sealed box fails closed; <t shares produce wrong secret detectable via BLAKE2b; v1 operator-readable at runtime documented honestly; no custom crypto; `pnpm audit` 2026-09-20 rerun: 0 new advisories for `@noble/ciphers@2.4.0`, `@noble/hashes@1.8.0`, `libsodium-wrappers@0.8.4`, `shamir-secret-sharing@0.0.4` (high/high vulns are `tar-fs`/`undici`/`vite` via `testcontainers`/`tailwind`, pre-existing, not introduced by T2.4); `pnpm --filter @heartbeat-vault/crypto typecheck` clean; 19 tests RED→GREEN.
  **Impact:** `packages/crypto/src/asymmetric.ts`, `sharing.ts`, `asymmetric.test.ts` (5 tests), `sharing.test.ts` (5 tests), `src/index.ts` (new exports), `package.json` (pinned deps), `DESIGN-NOTE.md` (ADR-006 draft + custody table + RED/GREEN), `docs/PROGRESS.md` T2.4 marked done.

- 2026-09-20 — **Decision:** Envelope AEAD = XChaCha20-Poly1305 via `@noble/ciphers@2.4.0` pinned exact; vitest@5.0.1 + @types/node@22.13.10 pinned; envelope `packages/crypto/src/envelope.ts` TDD RED→GREEN.
  **Date:** 2026-09-20
  **Context:** T2.2 Wave 2 — crypto envelope core blocks T2.3, T2.4, T9.1. Design `docs/DESIGN.md §4` + research `crypto-primitives-2026.md`.
  **Rationale:** Noble ciphers Cure53-audited, pure-TS ESM, 24 B random nonce (XChaCha) with 2^88 bound, constant-time `equalBytes`, no WASM, per-secret 32 B DEK memzero, AAD-bound wrap `{kid,kekVersion,tenantId,switchId}`, version-in-blob prefix. Single audit-proven primitive satisfies brief §6/§7.1 without custom crypto.
  **Alternatives Rejected:** AES-256-GCM with 12 B nonce (needs counter), libsodium-wrappers for envelope (WASM, less AAD control — kept for T2.4 sealed-box), crypto.subtle DIY (non-constant-time, no XChaCha).
  **Security Implications:** KEK caller-supplied 32 B, no persistence/KDF here; wrong AAD/KEK/tag/ciphertext/version all fail closed; DEKs zeroized in try/finally; secrets never logged. `pnpm audit` 2026-09-20: 0 new advisories on new deps; 2 pre-existing turbo advisories (GHSA-hcf7-66rw-9f5r, GHSA-3qcw-2rhx-2726, turbo 2.5.4 ≤2.9.13, patched ≥2.9.14) — tracked, not introduced by this change.
  **Impact:** `packages/crypto/src/envelope.ts`, `envelope.test.ts` (9 tests), `DESIGN-NOTE.md` (ADR-005 draft + RED output), `docs/PROGRESS.md` T2.2 marked done; blocks cleared for T2.3 (KEK rotation/Argon2) and T2.4 (asymmetric/Shamir).

- 2026-09-20 — **Decision:** Adopt user-answered Phase 0 defaults (single-household; Email+Webhook+Telegram; GH Actions+GHCR; English-only; permissive license family; data-at-rest first; server-side release; standard-only v1).
  **Context:** Phase 0 clarifying questions, all 8 answered with recommended defaults.
  **Rationale:** Matches self-hosted brief with smallest safe v1 scope.
  **Alternatives Rejected:** Multi-tenant HA now, Matrix/SFTP in v1, bilingual v1, recipient-held shares in v1, hardened in v1.
  **Security Implications:** Server-side KEK = operator-readable at runtime (documented honestly); shares/hardened deferred, not dropped.
  **Impact:** `docs/DESIGN.md` §§1–10; hardened tracked as TASK-002 next-development todo.

- 2026-09-18 — **Decision:** Adopt general-first `.claude/` agent infrastructure.
  **Context:** Blank repository, stack unknown.
  **Rationale:** A stack-agnostic scaffold lets any agent orient and contribute
  immediately, then self-update files as real knowledge emerges.
  **Alternatives Rejected:** Stack-specific scaffold (premature — stack unknown).
  **Security Implications:** None yet; security standards will gate the first commit.
  **Impact:** All future sessions follow `HOW_TO_RESUME.md` + `AGENT_RULES.md`.

- 2026-09-18 — **Decision:** Enforce Conventional Commits for all commit messages.
  **Context:** User requested a proper, enforced commit message structure.
  **Rationale:** Conventional Commits is tooling-friendly (changelogs, semver,
  searchability) and stack-agnostic, fitting the general-first setup.
  **Alternatives Rejected:** Custom/free-form format (inconsistent, not machine-readable).
  **Security Implications:** None; commit subjects must still never contain secrets.
  **Impact:** `CODING_STANDARDS.md` defines the format; `AGENT_RULES.md` enforces it;
  `new_feature` / `new_endpoint` / `bug_fix` templates check it at completion.

- 2026-09-20 — **Decision:** T3.1 bootstrap — API server scaffold + one-time setup token + permanent 410 — Framework `Fastify 5.6.2` pinned exact (default per plan, ADOPTED) + `@fastify/helmet@13.1.1` + `@fastify/cookie@11.1.1` + `zod@4.6.5` pinned exact; `fastify` chosen over `express`/`hono`/`koa` for production-hardened Node server with first-class TypeScript, `bodyLimit` 1 MB, `@fastify/helmet` security headers, `@fastify/cookie` strict session prep for T3.2, and lean plugin model that keeps `apps/api` a deep module (no deep framework coupling). `zod@4.6.5` (already in web) validates `POST /api/setup` `{token,email,password>=12,name}` with generic 400 without enumeration. `apps/api/src/lib/setup-token.ts` `generateSetupToken=32B crypto.randomBytes base64url` (≈43 chars, no `+/`) + `hashSetupToken=SHA-256 hex` 64 chars + `timingSafeEqual` constant-time verify (Buffer.from hex utf8 equal length guard). `apps/api/src/server.ts` `buildServer(pool|{pool})` factory (both overloads for spec `buildServer(db)` + options), Fastify `bodyLimit 1 MB`, `trustProxy`, `helmet` (contentSecurityPolicy false for API, keeps `x-content-type-options nosniff` + `x-frame-options`), `cookie`, `GET /api/health` 200 `{status:ok}`, delegates to `registerSetupRoutes`. `apps/api/src/routes/setup.ts` single-transaction `BEGIN ... FOR UPDATE ... COMMIT` atomic: SELECT `setup_completed`/`setup_token_hash`/`setup_token_expires_at` FOR UPDATE, if `completed=true`→ROLLBACK 410 gone, if hash empty→410, if `expires_at<=now`→410, if `timingSafeEqual(SHA256(token),storedHash)==false`→400 generic, else `hashPassword` (Argon2id via `@heartbeat-vault/crypto`) inserts `users` admin, upserts `setup_completed true` + clears `setup_token_hash` + sets `setup_token_consumed_at`, inserts `audit_log` `setup_completed` with SHA-256 hash chain seed, COMMIT 201 `{id,email,role:admin}`; duplicate email 23505→400 generic; never logs token; subsequent calls 410 permanent via config read. Tests `apps/api/src/setup.test.ts` 9 tests via Testcontainers `postgres:17-alpine` + PostgreSqlContainer (`@testcontainers/postgresql@10.14.0`, `testcontainers@10.14.0`, `vitest@5.0.1` pinned) with real Postgres `clock_timestamp()` + `drizzle-orm` migrator: health+helmet headers, happy→410+admin role+audit+cleared hash, wrong token 400 no user, expired 410/400 no user, weakPassword 400, invalidEmail 400, hash-only storage (hex64 == SHA256, no plaintext), permanent 410 unreachable after completion, token shape (base64url 43 chars, hex64). TDD RED→GREEN (RED: server/setup-token missing → import fail, GREEN 9/9 3.9s). `pnpm --filter @heartbeat-vault/api typecheck` clean strict (`@types/pg@8.11.0` + `@types/node@22.13.10`), `lint` clean, `prettier` 100-width singleQuote pass.
  **Date:** 2026-09-20
  **Context:** Plan Wave 4 T3.1 — depends on T2.1+T2.3 (done), blocks T3.2+T7.2; sibling T2.5 must not be touched (packages/crypto src untouched, only imported); Dockerfile placeholder `server.mjs` must stay (T10 wires real start). DESIGN.md §7 bootstrap: 32 B token, hashed SHA-256 in `app_config`, 60 min TTL, one-time admin, then 410 Gone forever.
  **Rationale:** Fastify 5.6.2 is the current LTS exact pinned (plan default: `Fastify 5 (pinned exact) + @fastify/cookie + zod`), aligns with `brief §7` typed API backend; helmet 13.1.1 is Fastify 5 companion (CSP off for JSON API, keeps HSTS/frame/noSniff), cookie 11.1.1 prepares T3.2 sessions; zod 4.6.5 reuses web validation and allows `safeParse` generic 400 without enumerating which field failed (prevents token oracle); `crypto.randomBytes` 32 B base64url is URL-safe for installer print, SHA-256 hex is deterministic 64-char hash for `app_config` (no plaintext at rest, installer holds plaintext once); `timingSafeEqual` on hex buffers (equal-length check first) prevents timing oracle on token compare; single `BEGIN ... SELECT ... FOR UPDATE ... COMMIT` gives serializable bootstrap (two concurrent POSTs: one wins, other sees `setup_completed=true` or hash empty → 410); `FOR UPDATE` on `app_config` rows prevents TOCTOU; Argon2id via `@heartbeat-vault/crypto` reuses T2.3 OWASP `m=19456,t=2,p=1` PHC; audit_log inserted in same TX before commit (non-repudiable bootstrap event); error mapping generic 400 hides whether token vs email vs password failed, 410 distinguishes gone-but-enumerable bootstrap-exhausted state (intentionally permanent, not brute-force).
  **Alternatives Rejected:** `express@4` + `helmet` (no built-in `bodyLimit`, callback-style, weaker TypeScript); `hono` (edge-first, less Postgres/cookie ecosystem for T3.2 sessions); `koa` (middleware ordering foot-guns, no `bodyLimit` guard); storing plaintext token in `app_config` (violates hash-only requirement — audit fails); separate `setup_token` table (overkill — `app_config` KV is the singleton config per T2.1); JWT-style token (needs signing key — overengineered for one-time bootstrap); `pg` `Client` per request (pool `connect` + `BEGIN`/`COMMIT`/`release` is the correct transactional pattern for node-postgres); opening `setup_token_expires_at` check outside TX (would be TOCTOU — must be inside `FOR UPDATE`).
  **Security Implications:** Secrets never logged (no `console.log(token)`); token hash-only storage verified by test (`storedHash === SHA256(token)`, `storedHash !== token`, length 64); constant-time `timingSafeEqual` prevents timing leak on token compare; `bodyLimit` 1 MB mitigates large-payload DoS; helmet gives `x-content-type-options nosniff` + `x-frame-options` + `hsts` + `x-dns-prefetch` for API responses; zod `email` + `password.min(12)` enforced before DB (generic 400 without field enumeration prevents oracle); transaction atomicity ensures partial admin creation cannot persist without config flip; `setup_token_hash` cleared to '' + `setup_completed=true` after success, second POST always 410 (tested 3 variants); `audit_log` hash `SHA256(setup:userId:now)` inserted in same commit (replay-detectable); `pnpm audit` 2026-09-20: api prod deps (`fastify@5.6.2`, `@fastify/helmet@13.1.1`, `@fastify/cookie@11.1.1`, `zod@4.6.5`, `drizzle-orm@0.45.2`, `pg@8.13.3`) 0 new high; dev `testcontainers@10.14.0` + `undici@5.29.0` 8 high /12 moderate /4 low are dev-only (not shipped to prod image, which will `npm prune --production` before T10); `turbo@2.5.4` GHSA-hcf7+GHSA-3qcw pre-existing (not introduced); no `as any`/`@ts-ignore`; Dockerfile untouched (verified no diff).
  **Impact:** `apps/api/package.json` (add `fastify@5.6.2`, `@fastify/helmet@13.1.1`, `@fastify/cookie@11.1.1`, `zod@4.6.5`, `pg@8.13.3`, `drizzle-orm@0.45.2`, workspace `*` for `@heartbeat-vault/crypto`+`@heartbeat-vault/db`, dev `@types/pg@8.11.0`/`@types/node@22.13.10`/`vitest@5.0.1`/`testcontainers@10.14.0`/`@testcontainers/postgresql@10.14.0`), `apps/api/src/server.ts`, `apps/api/src/lib/setup-token.ts`, `apps/api/src/routes/setup.ts`, `apps/api/src/setup.test.ts`, `apps/api/vitest.config.ts`, `docs/PROGRESS.md` T3.1, `pnpm-lock.yaml`; keeps `apps/api/src/index.ts` placeholder and `apps/api/Dockerfile` placeholder `server.mjs` (T10 real start); no sibling files (`packages/crypto/src`, `apps/web`, `docs/adr/`, compose) touched except lockfile/PROGRESS/DECISIONS_LOG + `apps/api/*` + new `apps/api/package.json` deps; staged only, no commit; unblocks T3.2 (login/sessions) and T7.2 (guided wizard reads 410 to gate wizard); T2.5 sibling unaffected.

- 2026-09-20 — **Decision:** T3.2 auth core — login/opaque sessions/rate-limit/lockout/audit chain — `apps/api/src/lib/audit.ts` genesis 32 zero bytes `hash=SHA256(prev|'|'|ts|'|'|actor|'|'|action|'|'|target)` via `writeAudit(client,{actorId,action,target,ip,requestId})` (SELECT hash ORDER BY id DESC LIMIT 1, inserts `ts`+`prev_hash`+`hash` in caller's TX), `computeAuditHash` for test recompute, `apps/api/src/lib/rate-limit.ts` hand-rolled <50 LOC per-IP token bucket 5/15min → 429 `Retry-After`, `apps/api/src/lib/auth-middleware.ts` `createAuthPreHandler(pool)` `__Host-session` → SHA256 hex → sessions+users revoked_at IS NULL expires_at>now 401 or attach `request.user`+`sessionTokenHash`, `apps/api/src/routes/auth.ts` `POST /api/login` zod email/password 400/401 generic `invalid_credentials` (no enumeration), per-IP check before DB, dummy PHC `$argon2id$v=19$m=19456,t=2,p=1$7xMTRCUAskrVin2v5I7PxQ$2zR8iIIQOAbaouop07+izMO8proVleYWEXh0Z0/vDvw` for unknown-email timing, locked_until>now generic 401, verifyPassword fail → BEGIN increment failed_attempts +15min lock at 5 + `auth_lockout` audit, success → BEGIN reset attempts 32B randomBytes base64url SHA256 hash sessions 24h + `auth_login` + `__Host-session` HttpOnly Secure SameSite Strict Path=/ expires, `POST /api/logout` + `GET /api/me` + `POST /api/sessions/revoke-all` revoke+clearCookie+auth_* audits, `apps/api/src/server.ts` wires `registerAuthRoutes`, fixes `apps/api/src/routes/setup.ts` remove `name` (schema no column, sloppy drop → delete, strip unknown via Zod), replace inline audit `SHA256(setup:userId:now)` with `writeAudit` chain, audit no `request.body` → `request.log` (only `request.log.error(err)`); `apps/api/src/auth.test.ts` 8 tests vitest+Testcontainers `postgres:17-alpine` (login happy cookie+session hash+24h+/me, bad pw vs unknown same 401 shape, /me 401/200, logout revokes, revoke-all kills all, 6th rapid 429 Retry-After per-IP, lockout 5 bad → locked_until+auth_lockout+generic 401 while locked, audit chain recompute); `pnpm audit` 2026-09-20 no new high on api deps (fastify/helmet/cookie/zod/pg/drizzle clean, pre-existing turbo/tar-fs/undici dev-only); `pnpm lint`+`typecheck` clean strict, 17/17 green.
  **Date:** 2026-09-20
  **Context:** Plan Wave 5 T3.2 — depends on T2.1+T2.3+T3.1 (done), blocks T3.3/T3.4; MUST DO fixes (a) setup audit chain (b) remove name (c) no password in logs, TDD RED→GREEN, typecheck+lint clean, no new deps (<80 LOC hand-rolled), no packages/crypto|db src change, only apps/api/*+lockfile/PROGRESS/DECISIONS_LOG.

- 2026-09-20 — **Decision:** T2.5 KAT + differential + tamper + 90% coverage gate — `packages/crypto/src/vectors.test.ts` 33 tests (XChaCha20-Poly1305 differential noble↔libsodium `crypto_aead_xchacha20poly1305_ietf_*` 4 tests incl. identical ciphertext, HKDF-SHA256 RFC 5869 §A.1 TC1 KAT `3cb25f25faacd57a90434f64d0362f2a2d2d0a90cf1a5a4c5db02d56ecc4c5bf34007208d5b887185865` pinned + differential vs `node:crypto hkdfSync` + random/empty, HMAC-SHA256 RFC 4231 TC1 `b0344c61d8db38535ca8afceaf0bf12b881dc200c9833da726e9376c2e32cff7` + TC2 `5bdcc146…` + differential vs `createHmac`, Argon2id @node-rs/argon2 hash→verify + hashRawSync + cross-impl to libsodium `crypto_pwhash_str_verify` with documented fallback — libsodium-wrappers 0.8.4 Emscripten build does NOT expose `crypto_pwhash*` (verified via `Object.keys(sodium)` — no pwhash entries), so differential is sync vs async @node-rs + PHC parse, never fabricated vector, noted in file header, Shamir round-trip 2-of-2/2-of-3/3-of-5 + share-length + quorum, Wycheproof-style tamper matrix 7 classes tag/ct/wrappedCT/nonce/nonce-length/AAD/key-length/version + oracle-discipline indistinguishable `decrypt failed`); coverage-fill 8 tests (deriveKEKAsync vs sync, KDF/rotation/asymmetric/sharing/envelope validation, dek-length crafted 16B via direct `xchacha20poly1305` wrap to hit `envelope.ts:239-240`); `vitest.config.ts` `coverage.provider=v8` `include src/**/*.ts` `exclude **/*.test.ts` `thresholds {lines:90 functions:90 branches:80 statements:90} all:true reporter text/lcov/html`; `@vitest/coverage-v8@5.0.1` pinned exact (matches vitest 5.0.1) + `test:coverage` scripts in `packages/crypto` (`vitest run --coverage`) and root (`turbo run test:coverage`) + `turbo.json` `test:coverage` task; `pnpm test:coverage` + `pnpm --filter @heartbeat-vault/crypto test` 64/64 green (31 existing +33 new), `vitest --coverage` 94.5% lines (94.57 stmts, 91.5 branch, 100 func) gate passes, `typecheck` strict clean.
  **Date:** 2026-09-20
  **Context:** Plan Wave 4 T2.5 — depends on T2.2-T2.4 (done), blocks T8.1/T12.1, sibling T3.1 bootstrap must not be touched (apps/api untouched). Brief §14 gate: crypto KAT + coverage threshold enforced. Expected outcome: `vectors.test.ts` hand-transcribed vectors (no network), strict TS, vitest thresholds, CI-blocking note in DESIGN-NOTE.md.
  **Rationale:** Hand-transcribed KATs only where memory-certain (HKDF RFC 5869 TC1, HMAC RFC 4231 TC1/TC2 — verified via `node --input-type=module` differential that noble vs node:crypto produce identical OKM/HMAC and match RFC hex; XChaCha IETF differential proves noble and libsodium interoperate byte-for-byte, better than a single pinned vector which could drift with impl; Argon2 cross-impl would be ideal but libsodium-wrappers 0.8.4 has no pwhash export — verified empirically, so we document fallback and prove correctness via @node-rs sync vs async + PHC round-trip, never fabricate a vector `differential > invented KAT`; Shamir has no external KAT — round-trip + commitment is the correct proof; tamper matrix covers every byte class per Wycheproof style and asserts generic `decrypt failed` without oracle detail; coverage-fill pushes `kdf.ts`/`rotation.ts`/`asymmetric.ts`/`sharing.ts` from 76% to 94.5% lines by exercising `deriveKEKAsync`, validation throw branches, `rotateKEK` fallback AAD + tryUnwrap failure, duplicate-key, short-sealed, Dek-length mismatch craft; `@vitest/coverage-v8@5.0.1` is the matching coverage provider for vitest 5.0.1 (v8 provider, pinned exact per SECURITY_STANDARDS); `all:true` ensures `index.ts` 0% barrel is counted but threshold still passes at 94.5 >90.
  **Alternatives Rejected:** Fetching Wycheproof JSON from GitHub (network fetches forbidden — hand-transcribed vectors + differential instead); `@vitest/coverage-istanbul` (v8 is default for Node 20+ and matches `vitest 5` docs); `c8` directly (vitest wraps it); per-file thresholds (global is the CI gate per brief §8); lowering threshold to 76% to make pre-fill pass (rejected — add coverage-fill to honestly reach 90); adding `age-encryption` or `argon2` secondary lib for second Argon2 vector (extra dep, not needed — same lib sync vs async is independent code path); `coverage.exclude` for `envelope.ts:254-256` dead branch (rejected — keep dead code uncovered and accept 94.5% rather than hide it; documented as unreachable `equalBytes` pre-check).
  **Security Implications:** No implementation files changed (tests + config only per MUST DO); no DB/API/UI code touched; no custom crypto; every vector labeled with source (RFC/section) in file header; no fabricated vector — where memory uncertain, differential used and noted; tamper matrix proves `decrypt` fails closed with generic message for every byte class (tag/ct/nonce/AAD/key/version) and oracle-discipline test proves indistinguishability (no byte-offset leak); Argon2id pepper fail-closed still enforced; envelope DEKs still zeroized; `pnpm audit` 2026-09-20: `@vitest/coverage-v8@5.0.1` has 0 new advisories (pre-existing `turbo@2.5.4` GHSA-hcf7-66rw-9f5r moderate + GHSA-3qcw-2rhx-2726 low, `undici`/`tar-fs`/`esbuild` via testcontainers dev-only, not introduced); `pnpm --filter @heartbeat-vault/crypto typecheck` strict clean (no `as any`/`@ts-ignore`), `pnpm --filter @heartbeat-vault/crypto build` green after readonly-cast fixes.
  **Impact:** `packages/crypto/src/vectors.test.ts` (new, 691 lines), `packages/crypto/vitest.config.ts` (coverage block), `packages/crypto/package.json` (`@vitest/coverage-v8@5.0.1` + `test:coverage`), root `package.json` (`test:coverage`), `turbo.json` (`test:coverage` task), `packages/crypto/DESIGN-NOTE.md` (§T2.5 + CI-blocking note), `docs/PROGRESS.md` T2.5 marked done, `pnpm-lock.yaml` updated; staged only, no commit; unblocks T8.1/T12.1; blocks sibling T3.1 unaffected.

- 2026-09-20 — **Decision:** T3.4 roles + admin-invite + RBAC + IDOR guards — `apps/api/src/lib/roles.ts` `requireRole('admin'|'user')` preHandler: rank table user=1 admin=2, unknown role rank 0 → 403 default-deny, admin rank 2 bypasses all, else rank must equal/exceed required; `requireSelfOrAdmin(paramName='id')`: admin bypass, self match passes, anyone else → 404 `not_found` (same shape as unknown-id response — no enumeration). `apps/api/src/routes/invites.ts`: POST /api/invites admin-only, zod `{email, role:'user'|'admin'}` generic 400, 32 B `crypto.randomBytes` base64url token, only SHA-256 hex hash stored in `invites.token_hash`, `expires_at = now()+24 h`, `invite_created` audit in TX, 201 `{id, token}` — plaintext token returned exactly once, never persisted; POST /api/invites/consume public + per-IP rate-limit 5/15 min → 429 Retry-After, zod `{token,email,password>=12}` (no `name` — removed T3.2, not reintroduced), atomic single-use: one TX does `SELECT ... FOR UPDATE` on `token_hash` + constant-time `timingSafeEqual` hash compare + `consumed_at IS NULL` + `expires_at > now` + case-insensitive email match (every failure → ROLLBACK + generic 400 `invalid_request`) → Argon2id `hashPassword` → INSERT users with invited role (23505 → 400) → `consumed_at=clock_timestamp()` → `invite_consumed` audit → COMMIT 201. `apps/api/src/routes/users.ts`: GET /api/users admin-only list id/email/role/created_at (sans password_hash); GET /api/users/:id self-or-admin, 404 for others same shape; POST /api/users/:id/revoke-sessions self-or-admin, revokes active sessions + `sessions_revoked` audit + clearCookie when self. `apps/api/src/routes/admin.ts`: PUT /api/admin/settings admin-only zod `{openRegistration:boolean}` upserts `app_config.open_registration` + `admin_settings_updated` audit in one TX. `apps/api/src/routes/register.ts`: POST /api/register gated on `app_config.open_registration` (default 'false' → 404 `not_found` indistinguishable from unknown route), open → zod → role='user' + `user_registered` audit. `apps/api/src/server.ts` wires all four. Tests `apps/api/src/invites.test.ts` 11 tests Testcontainers `postgres:17-alpine` + drizzle migrator; full api suite 28/28 GREEN (setup 9 + auth 8 + invites 11), `tsc --noEmit` strict clean, `eslint` clean.
  **Date:** 2026-09-20
  **Context:** Plan Wave 6 T3.4 — depends on T3.2 (done), blocks T4.1/T7.5/T8.4; sibling T3.3 owns migration 0001_totp_webauthn + two-factor files (untouched, not present); MUST NOT edit `packages/db/schema.ts` — raw SQL against existing `invites`/`app_config`/`users`/`sessions` tables from migration 0000; no new deps (pnpm-lock unchanged); session note: T3.4 implementation + tests found in working tree from a prior unfinished session; this session verified the design against the spec, removed residual `as string` type-escape casts + dead test var, and produced the verification evidence (GREEN 28/28, typecheck+lint clean). Prior-session RED run not independently witnessed — tests are behavioral (HTTP surface via `app.inject`, DB assertions) and fail on implementation regression, which is the load-bearing property.
  **Rationale:** Rank-ladder RBAC (unknown rank 0) makes role additions fail closed; 404-not-403 for self-or-admin failures hides resource existence (IDOR + enumeration guard in one); invite token follows the setup-token precedent (32 B CSPRNG base64url, hash-only storage, constant-time compare) so there is one audited token pattern; single-use atomicity lives entirely inside one `FOR UPDATE` TX so two concurrent consumes serialize on the row lock — second sees `consumed_at IS NOT NULL` → 400, no check-then-act window; consuming email must match invite email (case-insensitive) so a leaked token alone is insufficient; open registration default-deny 'false' + 404 (not 403/501) keeps closed instances indistinguishable from route-absent; every state change (invite created/consumed, settings toggle, session revoke, registration) is audited inside the same TX as the change.
  **Alternatives Rejected:** 403 for cross-user reads (leaks existence — 404 chosen); drizzle ORM in routes (requires `packages/db/schema.ts` edits owned by sibling T3.3 — raw SQL chosen); separate `invite_tokens` plaintext column (hash-only precedent); in-consume role override from request body (role must come from the admin's invite row, never the consumer); JWT/email-link invites (out of scope, self-hosted admin flow is direct-token); checking `consumed_at` outside TX (TOCTOU double-consume).
  **Security Implications:** Token plaintext never stored or logged (test asserts DB hash == SHA-256(token), 64 hex); `timingSafeEqual` on hash compare prevents timing oracle; generic `invalid_request` 400 on all consume failures hides which factor failed (token vs email vs expiry vs consumed); rate-limit 5/15 min per IP on consume throttles token brute-force (2^256 space); users list and detail never return `password_hash`; revoke-sessions audited with actor + target; settings toggle audited (`admin_settings_updated`) so registration-surface changes are non-repudiable; audit chain (T3.2) untouched and re-verified by auth tests; no secrets in logs (only `request.log.error(err)`); no new dependencies → no new audit surface; `pnpm audit` not re-run (zero dependency changes this session; last run T3.2 2026-09-20 clean of new highs on api deps).
  **Impact:** `apps/api/src/lib/roles.ts` (new), `apps/api/src/routes/invites.ts` (new), `apps/api/src/routes/users.ts` (new), `apps/api/src/routes/admin.ts` (new), `apps/api/src/routes/register.ts` (new), `apps/api/src/invites.test.ts` (new), `apps/api/src/server.ts` (route wiring), `docs/PROGRESS.md` (T3.4 done), `.claude/state/DECISIONS_LOG.md` (this entry); `packages/db/*`, `packages/crypto/*`, `apps/web/*`, `pnpm-lock.yaml` untouched; staged only, no commit; unblocks T4.1 (switches RBAC context), T7.5 (wizard), T8.4.

- 2026-09-20 — **Decision:** T3.3 two-factor auth — TOTP + WebAuthn/passkeys + recovery codes + step-up. Login step-up via session flag, not a separate pendingToken: TOTP-verified users get `sessions.totp_pending=true` + `202 {stepUp:'totp'}` + the same `__Host-session` cookie; `auth-middleware` rejects pending sessions with `403 totp_pending` by default and only 2FA routes opt in via `{allowTotpPending:true}`; `POST /api/2fa/totp/challenge` (or WebAuthn login-verify) flips the flag in a TX. TOTP verify window is past-step-only `epochTolerance=[30,0]` (current + previous step, never future) plus otplib `afterTimeStep` = `users.totp_last_counter` for strict replay rejection (a reused step is refused even inside its validity window, per RFC 6238 §5.2 posture); matched step = `result.epoch/30` persisted. TOTP secret at rest: envelope AEAD (ADR-005 XChaCha20-Poly1305 via `@heartbeat-vault/crypto`), kid `totp`, kekVersion 1, AAD `{tenantId:'default',switchId:'totp'}`, KEK = 32 B decoded from `MASTER_KEY` env (64-hex or base64, fail-closed at first use), canonical-JSON Envelope serialized into `users.totp_secret_encrypted` bytea. Recovery codes: 10× `XXXX-XXXX` over base32 `A-Z2-7` (byte&31 unbiased, 40-bit each), Argon2id-hashed individually (`hashPassword`), rotation = DELETE-then-INSERT in one TX, single-use enforced by `UPDATE ... SET used_at WHERE used_at IS NULL` rowCount inside the consume TX; `/api/login/recover` is public, 2fa-scoped rate-limited, dummy-Argon2 timing equalization on unknown email, generic `401 invalid_code` identical shape both paths. WebAuthn: `@simplewebauthn/server@13.3.3` pinned, rpID/origin from `WEBAUTHN_RP_ID`/`WEBAUTHN_ORIGIN` (dev defaults `localhost`/`https://localhost` — misconfig fails closed at origin verification, never opens), challenges stored per-session in `sessions.webauthn_challenge` as `{"challenge","issuedAt"}` JSON with 5-min TTL, `webauthn_user_id` lazily generated 32 B at first register-options, stored transports runtime-narrowed against the literal union (no casts).
  **Date:** 2026-09-20
  **Context:** Plan Wave 6 T3.3, depends on T3.2 (done), blocks T8.1; sibling T3.4 (roles/invites) ran in parallel and is done — coordination honored: T3.3 owns `routes/two-factor.ts` + `routes/totp.ts` + `routes/recovery.ts` + `routes/webauthn.ts` + `routes/webauthn-register.ts` + `lib/kek.ts` + `lib/totp-store.ts` + `lib/webauthn.ts` + `lib/step-up.ts` + migration `0001_totp_webauthn` (migration + schema.ts appends landed in a prior session, verified against spec this session); T3.4 files (`invites.ts`, `roles.ts`, `users.ts`, `admin.ts`, `register.ts`) untouched. Shared-file edits limited to additive/contract-preserving changes: `auth-middleware.ts` (optional opts param, additive `AuthUser` field), `rate-limit.ts` (optional scope param, `login` behavior unchanged), `auth.ts` (202 branch after password verification; all 401 shapes untouched per MUST NOT), `server.ts` (one wiring line), `schema.ts` (append-only appends already present, no edits this session).
  **Rationale:** Session-flag step-up beats a separate opaque pendingToken (one session object, one cookie, no second token lifecycle, the flag lives next to the session row it gates); past-step-only + `afterTimeStep` gives both clock-skew tolerance (one step back) and hard replay rejection without a challenge store; envelope AEAD reuses the single audited encryption path with tenant/switch AAD binding so the TOTP secret cannot be replayed across purposes if the DB leaks; recovery hashes are Argon2id (not SHA) because recovery codes are low-entropy (40-bit) and offline cracking resistance matters; consume-time candidate loop over ≤10 unused hashes bounds worst case ~1 s and avoids a plaintext lookup column; challenge TTL keyed to the session row prevents cross-session challenge replay with zero extra storage; `requireStepUp` is a composable preHandler so T4 heartbeat/panic routes get 2FA gating without reimplementing the check.
  **Alternatives Rejected:** Separate `pendingToken` returned in the 202 body (two token lifecycles to revoke, cookie already carries the session); symmetric TOTP window `±1 step` (accepts future steps — defeats replay-on-step-forward); storing the TOTP secret plaintext or AES-GCM hand-rolled (violates crypto rules — envelope AEAD only); SHA-256 recovery hashes (40-bit codes offline-crackable); challenge in an in-memory Map (lost on restart, breaks multi-instance); QR data-URL server-side (extra dep; `otpauthUrl` is sufficient for any client QR lib); bumping `fastify` 5.6.2→5.8.5 in this task (2 pre-existing high GHSA advisories on fastify itself, zero on otplib/simplewebauthn — bump touches every route, deferred to a dedicated change so sibling-wave work isn't destabilized mid-verification).
  **Security Implications:** TOTP secret never at rest in plaintext (test asserts stored bytea does not contain the returned base32 secret; only envelope ciphertext >100 B); secret never logged (only `request.log.error(err)` / `request.log.warn(err)` objects); recovery codes plaintext returned exactly once, hash-only in DB (test asserts `$argon2id$` prefix and no plaintext match); enroll re-issue resets `totp_verified_at` so a stale enrollment cannot be verified without the current secret; pending sessions are 403 on every non-2FA route (middleware default-deny) so step-up cannot be bypassed by route confusion; challenge/disable/regenerate/recover all sit behind the `2fa` limiter (10/15 min per IP) on top of lockout; disable requires password and wipes TOTP + recovery codes + releases pending sessions (no orphaned locked state); every state change audited in the same TX (`2fa_totp_enroll_started`, `2fa_totp_enabled`, `2fa_totp_challenge_ok`, `2fa_totp_disabled`, `2fa_recovery_regenerated`, `auth_recovery_login`, `2fa_webauthn_registered`, `2fa_webauthn_login_ok`, `auth_stepup_started`); WebAuthn counter persisted and lib-checked (cloned-authenticator detection); verify calls on WebAuthn wire payloads fail closed (`400 verification_failed` / `400 challenge_expired`) — two documented `as unknown as` casts pass untrusted payloads into the SimpleWebAuthn verifiers, which are the designated boundary validators and throw on any malformed field.
  **Impact:** New deps `otplib@13.5.0` + `@simplewebauthn/server@13.3.3` (exact-pinned, pnpm-lock updated); new files `apps/api/src/{two-factor.test.ts,lib/kek.ts,lib/totp-store.ts,lib/step-up.ts,routes/totp.ts,routes/recovery.ts,routes/webauthn.ts,routes/webauthn-register.ts}`; `routes/two-factor.ts` registrar re-exports `requireStepUp`; modified `lib/auth-middleware.ts`, `lib/rate-limit.ts`, `routes/auth.ts`, `server.ts`, `apps/api/package.json`; `docs/PROGRESS.md` T3.3 done; verification: full suite 46/46 GREEN (setup 9 + auth 8 + invites 11 + two-factor 18) incl. sibling T3.4 suite, `tsc --noEmit` strict clean, `eslint` clean, `pnpm audit` no new advisories from added deps; env vars to document at deploy: `MASTER_KEY` (required for 2FA enrollment/verification), `WEBAUTHN_RP_ID`, `WEBAUTHN_ORIGIN`; unblocks T8.1; staged only, no commit.
